<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Transformer：抛弃循环与卷积的序列建模</title>
    <url>/zh-blog/2026/08/08/Transformer%EF%BC%9A%E6%8A%9B%E5%BC%83%E5%BE%AA%E7%8E%AF%E4%B8%8E%E5%8D%B7%E7%A7%AF%E7%9A%84%E5%BA%8F%E5%88%97%E5%BB%BA%E6%A8%A1/</url>
    <content><![CDATA[<p>本文是对 <em>Attention Is All You Need</em>
的精读笔记。本文按照论文的结构，依次梳理其研究动机，模型架构中的Encoder-Decoder、Self-Attention、Multi-Head
Attention、三种
attention、前馈网络与位置编码、残差连接等、Self-Attention
优势论证，训练配置与实验结果，搭配了案例理解原理。</p>
<span id="more"></span>
<p>论文链接：</p>
<p><a href="https://arxiv.org/pdf/1706.03762">Attention Is All You
Need</a></p>
<h2 id="研究背景">研究背景</h2>
<p>在 Transformer 提出之前，序列建模的主流方法是 RNN，包括 LSTM、GRU
等变体。它们按照位置顺序递归计算隐藏状态 <span class="math inline">\(h_t
= f(h_{t-1}, x_t)\)</span>。</p>
<p>这种结构存在两个根本性缺陷。第一，无法并行计算，必须等前一步计算完成后才能继续计算下一步，因此训练效率较低。第二，长程依赖难以捕捉。两个距离较远的位置之间，信息需要经过多步传递，传播路径越长，信息越容易衰减。</p>
<p>Transformer 的核心思想是彻底舍弃循环和卷积结构，完全依靠 attention
机制建模序列中任意两个位置之间的依赖关系，从而同时解决并行计算和长程依赖这两个问题。</p>
<h2 id="模型架构与核心原理">模型架构与核心原理</h2>
<p>Transformer 仍然采用机器翻译中常见的 Encoder-Decoder 结构。Encoder
接收输入序列并将其编码为一组连续表示，Decoder
在这些表示的基础上自回归地生成输出序列。与此前模型不同的是，Transformer
不再使用 RNN 或 CNN 进行序列建模，而是将 Attention 作为 Encoder 和
Decoder 中最主要的信息交互方式。</p>
<p>整个模型由 6 层 Encoder 和 6 层 Decoder 堆叠而成。Encoder
负责不断更新输入序列中每个位置的表示，Decoder
一方面建模已经得到的输出序列，另一方面通过 Encoder-Decoder Attention
读取 Encoder 的结果，最后经过 Linear 和 Softmax 得到下一个 token
的概率分布。</p>
<pre><code class="highlight mermaid">flowchart LR

    I[&quot;输入序列&quot;]
    E[&quot;Embedding&lt;br/&gt;+ Positional Encoding&quot;]

    ENC[&quot;Encoder × 6&lt;br/&gt;Self-Attention + FFN&quot;]

    Z[&quot;Encoder 输出&lt;br/&gt;Z&quot;]

    D[&quot;Decoder × 6&lt;br/&gt;Masked Self-Attention&lt;br/&gt;Encoder-Decoder Attention&lt;br/&gt;FFN&quot;]

    O[&quot;Linear + Softmax&lt;br/&gt;输出概率&quot;]

    I --&gt; E
    E --&gt; ENC
    ENC --&gt; Z
    Z --&gt; D
    D --&gt; O

    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef encoder fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef memory fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef decoder fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef output fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111;

    class I,E input;
    class ENC encoder;
    class Z memory;
    class D decoder;
    class O output;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
<h3 id="encoder-decoder">Encoder-Decoder</h3>
<p>Encoder 由 6 个结构相同的 Encoder Layer
堆叠而成，每一层包含两个主要模块：Multi-Head Self-Attention 和
Position-wise Feed-Forward Network。Self-Attention
负责让序列中的不同位置交换信息，FFN
则分别对每个位置的表示进行进一步变换。两个模块外都带有 Residual
Connection 和 LayerNorm。</p>
<p>第一层 Encoder 接收输入 token 的 Embedding 与 Positional Encoding
之和，后续每一层则以前一层的输出作为输入。经过 6 层之后得到一组连续表示
<span class="math inline">\(Z=(z_1,z_2,\ldots,z_n)\)</span>。</p>
<p>这组表示会被提供给 Decoder。由于 Encoder 中每一层的 Self-Attention
都允许一个位置关注输入中的所有位置，因此最终的 <span
class="math inline">\(z_i\)</span> 已经不是单独第 <span
class="math inline">\(i\)</span> 个 token
的表示，而是融合了整个输入序列上下文之后的结果。</p>
<p>Decoder 同样由 6 层组成，但每个 Decoder Layer 比 Encoder 多一个
Attention 模块。首先是 Masked Multi-Head
Self-Attention，用来建模已经得到的输出序列；随后是 Encoder-Decoder
Attention，用来读取 Encoder 的输出；最后再经过 FFN。三个子层同样都使用
Residual Connection 和 LayerNorm。</p>
<p>Decoder 最后一层的输出经过 Linear 映射到词表大小，再通过 Softmax
转换成概率分布，从而预测下一个
token。这个生成过程本身会在后面的完整例子中再展开，这里主要关注
Transformer 内部各个模块是怎样组成的。</p>
<h3 id="attention">Attention</h3>
<p>Attention 是 Transformer 中最核心的计算机制。论文将 Attention
描述为从一个 Query 和一组 Key-Value Pair 到输出的映射：Query 与不同 Key
的匹配程度决定对应 Value 应该获得多大的权重，最终输出则是这些 Value
的加权和。</p>
<p>Transformer 使用的具体形式是 Scaled Dot-Product
Attention，并进一步通过 Multi-Head Attention
将这一计算扩展到多个不同的表示子空间。</p>
<h4 id="scaled-dot-product-attention">Scaled Dot-Product Attention</h4>
<p>Scaled Dot-Product Attention 的公式为 <span
class="math inline">\(\operatorname{Attention}(Q,K,V)=\operatorname{softmax}\left(\frac{QK^T}{\sqrt{d_k}}\right)V\)</span>，其中
Query 和 Key 的维度为 <span class="math inline">\(d_k\)</span>，Value
的维度为 <span class="math inline">\(d_v\)</span>。</p>
<p><span class="math inline">\(QK^T\)</span> 首先计算所有 Query 与所有
Key 之间的点积，得到不同位置之间的匹配分数。由于实际计算时会同时处理一组
Query、Key 和
Value，因此这里可以直接通过矩阵乘法一次得到全部位置之间的关系。</p>
<p>随后将点积结果除以 <span
class="math inline">\(\sqrt{d_k}\)</span>。论文指出，当 <span
class="math inline">\(d_k\)</span>
较大时，点积结果的绝对值也容易随之增大，使 Softmax
落入梯度很小的区域，因此通过这一缩放因子控制数值范围。</p>
<p>经过 Softmax 后，每个 Query 都得到一组归一化的 Attention
Weight。最后再用这些权重对 Value
进行加权求和，就得到这个位置新的表示。</p>
<p>因此 Q、K 和 V 承担的作用并不相同。Q 和 K
用来计算不同位置之间应该建立多强的联系，V
则包含最终被读取和重新组合的信息。Attention
的核心也不是选择一个唯一的位置，而是根据输入动态决定不同 Value
在当前输出中各自占多大比重。</p>
<h4 id="multi-head-attention">Multi-Head Attention</h4>
<p>论文没有直接使用一组 <span
class="math inline">\(d_{\text{model}}\)</span> 维的 Q、K、V 完成一次
Attention，而是分别对 Q、K、V
进行多组不同的线性投影，再在这些投影后的表示上并行计算 Attention。</p>
<p>第 <span class="math inline">\(i\)</span> 个 Attention Head 定义为
<span
class="math inline">\(\operatorname{head}_i=\operatorname{Attention}\left(QW_i^Q,KW_i^K,VW_i^V\right)\)</span>。</p>
<p>所有 Head 的结果随后被拼接起来，再经过一次线性投影，即 <span
class="math inline">\(\operatorname{MultiHead}(Q,K,V)=\operatorname{Concat}(\operatorname{head}_1,\ldots,\operatorname{head}_h)W^O\)</span>。</p>
<p>原始 Transformer 使用 8 个 Attention Head，并设置 <span
class="math inline">\(d_{\text{model}}=512\)</span>，<span
class="math inline">\(d_k=d_v=64\)</span>。</p>
<p>这里比较容易混淆的是，Multi-Head Attention 并不是把原来的 512
维表示直接切成 8 个固定区域。每个 Head 都有自己独立学习的 <span
class="math inline">\(W_i^Q\)</span>、<span
class="math inline">\(W_i^K\)</span> 和 <span
class="math inline">\(W_i^V\)</span>，因此会从完整输入表示中投影出一组自己的
64 维 Q、K、V。</p>
<p>不同 Head 因为使用不同的投影参数，可以在不同表示子空间中学习不同的
Attention 关系。8 个 Head 各自得到 64 维输出，拼接后重新得到 512
维表示，再通过 <span class="math inline">\(W^O\)</span>
进行融合。论文认为，这样能够让模型同时关注不同位置、不同表示子空间中的信息，而不是把所有关系都压缩到一次
Attention 中。</p>
<p>这些 Head 只属于当前的 Multi-Head Attention 模块。下一层 Transformer
会使用自己的参数重新计算新的 Q、K、V，因此不同层中的 Head
也是彼此独立学习的。</p>
<h4 id="transformer-中的三种-attention">Transformer 中的三种
Attention</h4>
<p>Scaled Dot-Product Attention 定义了 Attention
的具体计算方式，Multi-Head Attention
则把这一计算扩展到多个表示子空间。在 Transformer 的实际结构中，同样的
Multi-Head Attention 又根据 Q、K、V 的来源以及是否加入
Mask，被使用在三个不同的位置。</p>
<table style="width:100%;">
<colgroup>
<col style="width: 35%" />
<col style="width: 8%" />
<col style="width: 13%" />
<col style="width: 41%" />
</colgroup>
<thead>
<tr class="header">
<th>Attention</th>
<th>Query</th>
<th>Key / Value</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Encoder Self-Attention</td>
<td>Encoder</td>
<td>Encoder</td>
<td>建模输入序列内部关系</td>
</tr>
<tr class="even">
<td>Decoder Masked Self-Attention</td>
<td>Decoder</td>
<td>Decoder</td>
<td>建模已有输出，同时阻止访问未来位置</td>
</tr>
<tr class="odd">
<td>Encoder-Decoder Attention</td>
<td>Decoder</td>
<td>Encoder</td>
<td>根据 Decoder 当前状态读取输入信息</td>
</tr>
</tbody>
</table>
<p>Encoder Self-Attention 中，Q、K、V 都来自前一层 Encoder
的输出，因此每个位置都可以关注前一层中的所有位置。第一层的 Q、K、V
则由加入位置编码后的输入表示产生。</p>
<p>Decoder Masked Self-Attention 的 Q、K、V 同样来自
Decoder，但会额外加入 Mask，使第 <span class="math inline">\(i\)</span>
个位置只能访问当前位置以及之前的位置。实现时，未来位置对应的 Attention
Score 会在 Softmax 之前被设为 <span
class="math inline">\(-\infty\)</span>，因此最终权重变为
0。这样可以保证模型在预测当前位置时不会提前使用未来输出的信息。</p>
<p>Encoder-Decoder Attention 后来通常也称为 Cross-Attention。这里的
Query 来自 Decoder，而 Key 和 Value 来自 Encoder 最终输出 <span
class="math inline">\(Z\)</span>。因此 Decoder 可以根据当前的表示与
Encoder 中不同位置进行匹配，并从输入序列中读取当前预测需要的信息。</p>
<p>三者并不是三套不同的 Attention 数学机制。它们底层使用的仍然是相同的
Scaled Dot-Product Attention 和 Multi-Head Attention，区别主要在于
Q、K、V 来自哪里，以及是否需要 Mask。</p>
<h3 id="transformer-的其他组成">Transformer 的其他组成</h3>
<p>除了 Attention 之外，Transformer 还包含 FFN、Embedding、Positional
Encoding、Residual Connection 和
LayerNorm。这些部分并不是论文最主要的创新，但共同构成了完整的网络结构。</p>
<p><strong>FFN</strong></p>
<p>每一层 Encoder 和 Decoder 都包含一个 Position-wise Feed-Forward
Network。它由两次线性变换和中间的 ReLU 组成，即 <span
class="math inline">\(\operatorname{FFN}(x)=\max(0,xW_1+b_1)W_2+b_2\)</span>。原始
Transformer 中输入和输出维度都是 512，中间隐藏层维度为 2048。</p>
<p>所谓 Position-wise，是指 FFN
会分别作用于序列中的每一个位置，同一层中所有位置共享相同的 FFN
参数，但不同 Transformer Layer 使用不同参数。因此 Attention
负责不同位置之间的信息交互，而 FFN
本身不再混合位置，只对每个位置已经获得的表示进行独立变换。</p>
<p><strong>Embedding 与输出层</strong></p>
<p>输入和输出 token 首先都会通过 learned embedding 转换为 <span
class="math inline">\(d_{\text{model}}=512\)</span> 维向量。Decoder
最终得到的隐藏表示则经过一个线性变换映射到词表维度，再通过 Softmax
得到下一个 token 的概率。</p>
<p>论文采用了参数共享，即全局使用一致的文本-向量转换，Encoder
Embedding、Decoder Embedding 和最终 Softmax
之前的线性变换共享同一套权重矩阵。在 Embedding
中使用这组权重时，还会额外乘以 <span
class="math inline">\(\sqrt{d_{\text{model}}}\)</span>。</p>
<p><strong>Positional Encoding</strong></p>
<p>由于 Transformer 完全舍弃了 recurrence 和
convolution，模型本身没有通过计算顺序获得 token
位置信息，因此作者额外向输入表示中加入 Positional Encoding。位置编码与
Embedding 具有相同的 <span
class="math inline">\(d_{\text{model}}\)</span>
维度，两者可以直接相加。</p>
<p>原始 Transformer 使用不同频率的正弦和余弦函数：<span
class="math inline">\(PE_{(pos,2i)}=\sin\left(\frac{pos}{10000^{2i/d_{\text{model}}}}\right)\)</span>，<span
class="math inline">\(PE_{(pos,2i+1)}=\cos\left(\frac{pos}{10000^{2i/d_{\text{model}}}}\right)\)</span>。其中
<span class="math inline">\(pos\)</span> 表示序列位置，<span
class="math inline">\(i\)</span>
表示表示向量中的维度。不同维度使用不同频率，使不同位置具有不同的位置表示。</p>
<p>作者选择这种形式的一个考虑是，对于固定的位置偏移 <span
class="math inline">\(k\)</span>，<span
class="math inline">\(PE_{pos+k}\)</span> 可以表示为 <span
class="math inline">\(PE_{pos}\)</span>
的线性函数，因此可能更容易学习相对位置关系。论文同时测试了 learned
positional
embedding，两者实验结果基本相同，最终选择正弦位置编码，是因为作者认为它可能更容易泛化到训练时没有见过的更长序列。</p>
<p><strong>Residual Connection 与 LayerNorm</strong></p>
<p>Transformer 在每一个子层外都加入了 Residual
Connection，并在残差相加之后进行 LayerNorm，即 <span
class="math inline">\(\operatorname{LayerNorm}\left(x+\operatorname{Sublayer}(x)\right)\)</span>。这里的
Sublayer 可以是 Multi-Head Attention 或 FFN。Residual Connection
使子层学习到的结果不会直接替代原始表示，而是在原输入的基础上进行更新；LayerNorm
则用于对得到的表示进行归一化。</p>
<p>由于残差连接要求输入 <span class="math inline">\(x\)</span>
和子层输出具有相同维度，因此 Transformer 中各个子层以及 Embedding
的输出都统一保持 <span
class="math inline">\(d_{\text{model}}=512\)</span>。原始论文采用的是先进行子层计算和残差相加、再进行
LayerNorm 的结构，也就是后来通常所说的 Post-Norm。</p>
<h3 id="self-attention的优势">Self-Attention的优势</h3>
<p>前面介绍了 Transformer 如何使用 Self-Attention
完成序列中的信息交互，论文随后进一步讨论了为什么选择 Self-Attention
来替代此前常用的 RNN 和
CNN。作者主要从三个方面进行比较：每一层的计算复杂度、能够并行处理的程度，以及序列中不同位置之间的信息传播路径长度。</p>
<p>论文中的比较结果如下：</p>
<table>
<colgroup>
<col style="width: 29%" />
<col style="width: 23%" />
<col style="width: 24%" />
<col style="width: 22%" />
</colgroup>
<thead>
<tr class="header">
<th>Layer Type</th>
<th>Complexity per Layer</th>
<th>Sequential Operations</th>
<th>Maximum Path Length</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Self-Attention</td>
<td><span class="math inline">\(O(n^2d)\)</span></td>
<td><span class="math inline">\(O(1)\)</span></td>
<td><span class="math inline">\(O(1)\)</span></td>
</tr>
<tr class="even">
<td>Recurrent</td>
<td><span class="math inline">\(O(nd^2)\)</span></td>
<td><span class="math inline">\(O(n)\)</span></td>
<td><span class="math inline">\(O(n)\)</span></td>
</tr>
<tr class="odd">
<td>Convolutional</td>
<td><span class="math inline">\(O(knd^2)\)</span></td>
<td><span class="math inline">\(O(1)\)</span></td>
<td><span class="math inline">\(O(\log_k n)\)</span></td>
</tr>
<tr class="even">
<td>Restricted Self-Attention</td>
<td><span class="math inline">\(O(rnd)\)</span></td>
<td><span class="math inline">\(O(1)\)</span></td>
<td><span class="math inline">\(O(n/r)\)</span></td>
</tr>
</tbody>
</table>
<p>其中 <span class="math inline">\(n\)</span> 表示序列长度，<span
class="math inline">\(d\)</span> 表示每个位置的表示维度，<span
class="math inline">\(k\)</span> 表示卷积核大小，<span
class="math inline">\(r\)</span> 表示受限 Self-Attention
中每个位置能够关注的邻域大小。</p>
<p>首先从计算复杂度来看，Self-Attention 每层需要计算序列中不同位置之间的
Attention，因此复杂度为 <span
class="math inline">\(O(n^2d)\)</span>；RNN 每个位置都需要对 <span
class="math inline">\(d\)</span> 维隐藏表示进行变换，复杂度为 <span
class="math inline">\(O(nd^2)\)</span>。因此 Self-Attention
并不是在任何情况下都具有更低的理论计算量。当序列长度满足 <span
class="math inline">\(n&lt;d\)</span> 时，<span
class="math inline">\(n^2d&lt;nd^2\)</span>，Self-Attention 才会比
Recurrent Layer 更有优势。论文指出，在当时机器翻译模型使用的 sentence
representation
中，序列长度通常确实小于表示维度，因此这一条件在实际任务中比较常见。</p>
<p>Self-Attention 更明显的优势在于并行性。RNN 的隐藏状态满足类似 <span
class="math inline">\(h_t=f(h_{t-1},x_t)\)</span> 的递归关系，第 <span
class="math inline">\(t\)</span> 个位置必须等待第 <span
class="math inline">\(t-1\)</span> 个位置计算完成，因此处理长度为 <span
class="math inline">\(n\)</span> 的序列至少需要 <span
class="math inline">\(O(n)\)</span> 次顺序计算。Self-Attention
则可以把所有位置的 Query、Key 和 Value
组织成矩阵，通过一次矩阵运算同时计算不同位置之间的
Attention，因此一层内部所需要的顺序操作数为 <span
class="math inline">\(O(1)\)</span>。这里的 <span
class="math inline">\(O(1)\)</span> 并不是说整个 Transformer
只有一步计算，而是说一层 Self-Attention 内部的串行步骤不会随着序列长度
<span class="math inline">\(n\)</span> 增长。不同 Transformer Layer
之间仍然需要依次计算，但同一层中的所有序列位置可以并行处理。这也是
Transformer 相比 RNN 更适合利用 GPU 进行大规模矩阵计算的重要原因。</p>
<p>第三个比较标准是长程依赖的信息传播路径。论文认为，一个位置的信息需要经过的中间计算步骤越少，模型就越容易学习两个远距离位置之间的依赖关系。在
RNN
中，一个位置的信息要影响距离较远的另一个位置，需要沿隐藏状态逐步传播，因此两个位置之间的最大路径长度为
<span
class="math inline">\(O(n)\)</span>。序列越长，远距离信息需要经过的中间步骤也越多。Self-Attention
则允许任意两个位置在同一层中直接建立联系。无论两个 token
在序列中相邻，还是相隔几十个位置，它们都可以通过一次 Attention
直接交换信息，因此最大路径长度为 <span
class="math inline">\(O(1)\)</span>。这也是论文认为 Self-Attention
更适合学习长程依赖的主要原因之一。</p>
<p>卷积网络同样可以并行计算，但单个卷积层通常只能连接局部位置。如果卷积核宽度为
<span
class="math inline">\(k&lt;n\)</span>，一个位置需要经过多层卷积才能获得远距离位置的信息。对于普通连续卷积，需要堆叠约
<span class="math inline">\(O(n/k)\)</span> 层才能连接任意两个位置；使用
dilated convolution 时，这一路径可以缩短到 <span
class="math inline">\(O(\log_k n)\)</span>，但仍然长于 Self-Attention
的直接连接。卷积层本身的计算开销也比较高。普通卷积的复杂度为 <span
class="math inline">\(O(knd^2)\)</span>，论文指出通常会比 Recurrent
Layer 多出约一个与卷积核大小 <span class="math inline">\(k\)</span>
有关的因子。使用 separable convolution 可以将复杂度降低到 <span
class="math inline">\(O(knd+nd^2)\)</span>；即使令 <span
class="math inline">\(k=n\)</span>，其计算复杂度也大致相当于 Transformer
中一个 Self-Attention Layer 与一个 Position-wise FFN 的组合。</p>
<p>当然，Self-Attention 的 <span class="math inline">\(O(n^2d)\)</span>
复杂度意味着，当序列非常长时，两两位置之间的 Attention
计算也会变得昂贵。论文因此提出，可以把每个位置的 Attention 限制在大小为
<span class="math inline">\(r\)</span> 的局部邻域内，使计算复杂度下降到
<span
class="math inline">\(O(rnd)\)</span>。代价是任意两个位置之间不再能够直接连接，最大路径长度会增加到
<span class="math inline">\(O(n/r)\)</span>。作者在论文中将这种
Restricted Self-Attention 作为未来值得继续研究的方向。</p>
<p>除了计算复杂度、并行性和路径长度之外，论文还提到 Self-Attention
可能带来一定的可解释性。作者观察模型学习得到的 Attention Distribution
后发现，不同 Attention Head 会表现出不同的行为，其中一些 Head 的
Attention Pattern 与句子的句法和语义结构存在明显联系。例如部分 Head
会关注长距离依赖，另一些 Head
则表现出与指代关系等语言结构相关的模式。</p>
<p>因此，论文选择 Self-Attention
并不是因为它在所有指标上都拥有更低的计算复杂度，而是因为它在当时典型的机器翻译序列长度下具有较好的计算效率，同时能够把序列位置之间的串行计算从
<span class="math inline">\(O(n)\)</span> 降到 <span
class="math inline">\(O(1)\)</span>，并将任意两个位置之间的信息传播路径缩短到
<span class="math inline">\(O(1)\)</span>。相比 RNN 的顺序递归和 CNN
的局部信息传播，Self-Attention
使整个序列能够并行计算，并让远距离位置直接交换信息，这构成了 Transformer
用 Attention 取代 recurrence 和 convolution 的主要理由。</p>
<h2 id="transformer-翻译样例">Transformer 翻译样例</h2>
<p>前面分别介绍了 Encoder、Self-Attention、Multi-Head Attention、FFN
等主要模块，但这些部分真正拼在一起之后，数据究竟如何从输入一路走到最终输出，其实并不那么直观。这里就用一次完整的机器翻译过程，把前面那些模块重新串起来看一遍。</p>
<p>假设一个已经训练好的 Transformer 收到英文输入：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">I love cats</span><br></pre></td></tr></table></figure>
<p>希望最终生成中文：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">我 爱 猫</span><br></pre></td></tr></table></figure>
<p>整个过程可以分成两个阶段：Encoder 先对完整输入序列做一次编码，把每个
token 变成融合了上下文的表示；Decoder 再以这些表示为条件，从
<code>&lt;BOS&gt;</code> 开始逐步预测目标序列中的下一个 token，直到生成
<code>&lt;EOS&gt;</code>。</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 65
  &#125;
&#125;&#125;%%

flowchart 

    I[&quot;输入序列&lt;br/&gt;I · love · cats&quot;]

    E[&quot;Embedding&lt;br/&gt;+ Positional Encoding&quot;]

    ENC[&quot;Encoder × 6&lt;br/&gt;Multi-Head Self-Attention&lt;br/&gt;+ FFN&quot;]

    Z[&quot;Encoder 最终表示 Z&lt;br/&gt;z_I · z_love · z_cats&quot;]

    BOS[&quot;Decoder 初始输入&lt;br/&gt;&amp;lt;BOS&amp;gt;&quot;]

    DEC[&quot;Decoder × 6&lt;br/&gt;Masked Self-Attention&lt;br/&gt;+ Cross-Attention + FFN&quot;]

    P[&quot;Linear + Softmax&lt;br/&gt;预测下一个 token&quot;]

    Y[&quot;我 → 爱 → 猫 → &amp;lt;EOS&amp;gt;&quot;]

    I --&gt; E
    E --&gt; ENC
    ENC --&gt; Z

    BOS --&gt; DEC
    Z --&gt; DEC
    DEC --&gt; P
    P --&gt; Y

    Y -. &quot;已生成 token&lt;br/&gt;继续加入 Decoder 输入&quot; .-&gt; DEC

    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef encoder fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef memory fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef decoder fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef output fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111;

    class I,E input;
    class ENC encoder;
    class Z memory;
    class BOS,DEC decoder;
    class P,Y output;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
<h3 id="encoder">Encoder</h3>
<p>输入序列先经过 token embedding，再与对应的位置编码相加。原始
Transformer 的模型维度是 <span
class="math inline">\(d_{\text{model}}=512\)</span>，所以
<code>I</code>、<code>love</code> 和 <code>cats</code> 各自得到一个 512
维的向量，合在一起就是 <span class="math inline">\(X^0\in\mathbb
R^{3\times512}\)</span>。这 3 对应序列里的三个位置。随后 <span
class="math inline">\(X^0\)</span> 会依次进入由 6 个结构相同、参数独立的
Encoder Layer 组成的堆叠。</p>
<p>在第一个 Encoder Layer 里，<span class="math inline">\(X^0\)</span>
先进入 Multi-Head Self-Attention。原始模型使用 8 个 head，每个 head
都从完整的 512 维表示出发，通过自己独立的线性投影得到 64 维的 Query、Key
和 Value。第 <span class="math inline">\(i\)</span> 个 head 可以写成</p>
<p><span class="math display">\[
Q_i=X^0W_i^Q,\quad
K_i=X^0W_i^K,\quad
V_i=X^0W_i^V
\]</span></p>
<p>然后计算</p>
<p><span class="math display">\[
\operatorname{Attention}(Q_i,K_i,V_i)
=
\operatorname{softmax}
\left(
\frac{Q_iK_i^T}{\sqrt{d_k}}
\right)V_i
\]</span></p>
<p>因为输入只有三个位置，<span class="math inline">\(Q_iK_i^T\)</span>
会得到一个 <span class="math inline">\(3\times3\)</span>
的矩阵，本质上就是当前 head 里三个 token 两两之间的匹配程度。以
<code>love</code> 这个位置为例，它会用自己的 Query 去和
<code>I</code>、<code>love</code>、<code>cats</code> 三个位置的 Key
做匹配。经过 scale 和 Softmax 之后，假设某个 head 得到的权重是
0.30、0.20、0.50，那么这个 head 对 <code>love</code> 的新表示就是三个
Value 按这个权重的加权和。Self-Attention 并不是简单地挑一个最相关的
token，而是根据当前输入动态算出一组权重，再按这些权重把不同位置的 Value
重新混合起来。</p>
<p>8 个 head
并行做同样的事，但由于各自的投影矩阵不同，它们其实是在不同的低维空间里观察
token 之间的关系。把 8 个 head 的 <span
class="math inline">\(3\times64\)</span> 输出拼接回去，重新得到 <span
class="math inline">\(3\times512\)</span> 的表示，再经过输出投影矩阵
<span class="math inline">\(W^O\)</span> 做一次融合。</p>
<p>Multi-Head Attention 之后是残差连接和 LayerNorm，然后进入
position-wise 的 FFN。原始 Transformer 里 FFN 对每个位置单独做一次 <span
class="math inline">\(512\rightarrow2048\rightarrow\operatorname{ReLU}\rightarrow512\)</span>
的变换。可以把一个 Encoder Layer 简单理解成：Attention
负责不同位置之间交换信息，FFN 负责每个位置自己加工当前的表示。</p>
<p>第一个 Encoder Layer 结束后得到 <span
class="math inline">\(X^1\)</span>，它继续进入第二个 Encoder
Layer。上一层内部算出来的 Q、K、V 并不会直接传下去，第二层接收的是新的
<span class="math inline">\(X^1\)</span>，再用这一层自己的参数重新生成
Q、K、V。整个 Encoder 就是这样一层一层往下走：<span
class="math inline">\(X^0\)</span> 经过第 1 层得到 <span
class="math inline">\(X^1\)</span>，再经过第 2 层得到 <span
class="math inline">\(X^2\)</span>，一直到第 6 层，最终得到 <span
class="math inline">\(Z=X^6\)</span>。</p>
<p>最终得到的 <span
class="math inline">\(Z=[z_I,z_{\text{love}},z_{\text{cats}}]\)</span>
里，三个输入位置仍然完整保留，每个位置还是一个 512
维表示，只是已经经过六轮 Self-Attention 和
FFN，不再是最初孤立的词向量，而是融合了整个输入序列上下文之后的表示。此时的
<span class="math inline">\(z_{\text{love}}\)</span> 里已经包含了模型从
<code>I</code> 和 <code>cats</code> 等位置交互过来的信息。</p>
<p>需要特别注意的是，Encoder 最终交给 Decoder 的并不是某一层 Attention
内部用过的 Q、K、V，而是第 6 层最终得到的 <span
class="math inline">\(Z\)</span>。Q、K、V 只是每一层 Attention
内部为了计算信息交换方式而临时产生的中间量。</p>
<h3 id="decoder">Decoder</h3>
<p>Encoder 只需要对输入序列计算一次。得到 <span
class="math inline">\(Z\)</span>
之后，在之后生成整个目标序列的过程中，源序列不会再变，因此同一个 <span
class="math inline">\(Z\)</span> 会一直作为 Decoder Cross-Attention
的信息来源。</p>
<p>最开始目标侧还没有任何实际 token，所以先向 Decoder 输入起始符
<code>&lt;BOS&gt;</code>。它同样经过 embedding 和位置编码，得到一个 512
维表示，进入 Decoder 第 1 层。</p>
<p>每个 Decoder Layer 包含三个主要模块：Masked Multi-Head
Self-Attention、Cross-Attention、FFN，每个子层之后同样有残差连接和
LayerNorm。</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 65
  &#125;
&#125;&#125;%%

flowchart LR

    D[&quot;Decoder 当前表示&quot;]

    S[&quot;Masked Multi-Head&lt;br/&gt;Self-Attention&quot;]

    C[&quot;Multi-Head&lt;br/&gt;Cross-Attention&quot;]

    F[&quot;FFN&quot;]

    O[&quot;该层 Decoder 输出&quot;]

    Z[&quot;Encoder 最终表示 Z&quot;]

    D --&gt; S
    S --&gt; C
    Z --&gt; C
    C --&gt; F
    F --&gt; O

    classDef decoder fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef encoder fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef ffn fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111;

    class D,S,C,O decoder;
    class Z encoder;
    class F ffn;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
<p>首先是 Masked Self-Attention。因为此时目标侧只有
<code>&lt;BOS&gt;</code> 一个位置，它暂时没有其他历史 token
可以读取，只能关注自己。经过这一部分以及对应的 Add &amp; Norm 后，得到
<code>&lt;BOS&gt;</code> 当前新的表示。</p>
<p>随后进入 Cross-Attention。这里 Q、K、V 的来源和 Self-Attention
不同：Query 来自 Decoder 当前的表示，Key 和 Value 来自 Encoder 最终表示
<span class="math inline">\(Z\)</span>。Decoder 的 Cross-Attention
有自己独立的投影参数，会根据当前层的需要重新把 Decoder 表示投影成
Query，把 <span class="math inline">\(Z\)</span> 投影成 Key 和
Value。以第 <span class="math inline">\(i\)</span> 个 head 为例，</p>
<p><span class="math display">\[
Q_i=AW_i^Q,\quad
K_i=ZW_i^K,\quad
V_i=ZW_i^V
\]</span></p>
<p>其中 <span class="math inline">\(A\)</span> 是经过当前 Decoder Masked
Self-Attention 后得到的表示。Decoder 的 Query 会与
<code>I</code>、<code>love</code>、<code>cats</code> 对应的 Key
做匹配，得到一组权重，再按这些权重从 Encoder 提供的 Value
里读取信息。真正的 attention 权重由输入内容、Decoder
当前状态和已经学到的参数共同决定，不同
head、不同层完全可以形成不一样的分布。</p>
<p>Cross-Attention 得到的信息经过残差和 LayerNorm 后进入
FFN。到这里，Decoder 第 1
层处理结束，只得到一个新的隐藏表示，此时模型还没有生成“我”。</p>
<p>这个表示会继续进入第 2 层，第 2 层重新执行自己独立的 Masked
Self-Attention、Cross-Attention 和 FFN，并再次读取同一个 <span
class="math inline">\(Z\)</span>。虽然各层读到的原始 <span
class="math inline">\(Z\)</span>
相同，但不同层有自己的投影参数，实际得到的 Query、Key、Value
以及最终读到的信息都可以不同。这个过程一直重复到第 6 层，只有完整经过 6
个 Decoder Layer 之后，最终的隐藏表示才会被送入输出层。</p>
<p>原始 Transformer 先通过一个 Linear 把 512
维隐藏表示映射到整个目标词表（假设词表大小是 50,000），得到 50,000 个
logits，再经过 Softmax 转成概率分布。模型据此预测出第一个
token：<code>我</code>。</p>
<p>从这个角度看，Decoder 最终做的其实是一次针对整个目标词表的 next-token
prediction。前面的 Masked Self-Attention、Cross-Attention、FFN
和多层变换，都是在不断构造一个更适合当前预测的隐藏表示，而最后的 Linear
+ Softmax
始终在回答同一个问题：在当前输入序列和已经生成的目标序列条件下，下一个
token 最应该是什么？</p>
<p>第一个 token <code>我</code> 生成之后，会被加入 Decoder
输入，下一轮变成
<code>&lt;BOS&gt; 我</code>。这段目标侧前缀重新完整走一遍 6 层
Decoder。此时 Masked Self-Attention
开始真正利用已经生成的历史：<code>我</code> 所在的位置可以读取
<code>&lt;BOS&gt;</code> 和自身的信息，而 Cross-Attention
仍然持续读取同一个 <span class="math inline">\(Z\)</span>。经过第 6
层后，取最后一个位置对应的隐藏表示，再经 Linear + Softmax，得到第二个
token：<code>爱</code>。</p>
<p>之后继续重复同样的过程：先是 <code>&lt;BOS&gt;</code> 经过 Decoder
得到 <code>我</code>，再把 <code>我</code> 拼进去得到
<code>&lt;BOS&gt; 我</code>，再经过 Decoder 得到 <code>爱</code>；接着是
<code>&lt;BOS&gt; 我 爱</code> 得到 <code>猫</code>；最后
<code>&lt;BOS&gt; 我 爱 猫</code> 得到
<code>&lt;EOS&gt;</code>。当模型预测出 <code>&lt;EOS&gt;</code>
时，本次翻译结束。</p>
<p>整个生成过程可以进一步表示如下：</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 65
  &#125;
&#125;&#125;%%

flowchart 

    X[&quot;I · love · cats&quot;]
    E[&quot;Embedding + Position&quot;]
    ENC[&quot;Encoder × 6&quot;]
    Z[&quot;Z&lt;br/&gt;z_I · z_love · z_cats&quot;]

    D1[&quot;&amp;lt;BOS&amp;gt;&quot;]
    D2[&quot;&amp;lt;BOS&amp;gt; 我&quot;]
    D3[&quot;&amp;lt;BOS&amp;gt; 我 爱&quot;]
    D4[&quot;&amp;lt;BOS&amp;gt; 我 爱 猫&quot;]

    DEC1[&quot;Decoder × 6&quot;]
    DEC2[&quot;Decoder × 6&quot;]
    DEC3[&quot;Decoder × 6&quot;]
    DEC4[&quot;Decoder × 6&quot;]

    Y1[&quot;我&quot;]
    Y2[&quot;爱&quot;]
    Y3[&quot;猫&quot;]
    Y4[&quot;&amp;lt;EOS&amp;gt;&quot;]

    X --&gt; E
    E --&gt; ENC
    ENC --&gt; Z

    D1 --&gt; DEC1 --&gt; Y1
    D2 --&gt; DEC2 --&gt; Y2
    D3 --&gt; DEC3 --&gt; Y3
    D4 --&gt; DEC4 --&gt; Y4

    Z --&gt; DEC1
    Z --&gt; DEC2
    Z --&gt; DEC3
    Z --&gt; DEC4

    Y1 -. &quot;加入下一轮输入&quot; .-&gt; D2
    Y2 -. &quot;加入下一轮输入&quot; .-&gt; D3
    Y3 -. &quot;加入下一轮输入&quot; .-&gt; D4

    classDef encoder fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef memory fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef decoder fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef output fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111;

    class X,E,ENC encoder;
    class Z memory;
    class D1,D2,D3,D4,DEC1,DEC2,DEC3,DEC4 decoder;
    class Y1,Y2,Y3,Y4 output;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
<p>从整体数据流来看，一个已经训练完成的 Transformer
可以概括成两部分：Encoder 先对完整输入序列计算一次，通过多轮
Self-Attention 和 FFN 把每个输入 token 变成融合上下文的表示 <span
class="math inline">\(Z\)</span>；Decoder
随后以已经生成的目标序列和同一个 <span class="math inline">\(Z\)</span>
为条件，通过 Masked Self-Attention、Cross-Attention 和 FFN
逐步形成当前预测位置的隐藏表示，再经过 Linear 和 Softmax 完成一次
next-token prediction。生成出的 token 会继续加入 Decoder
输入，模型重复这一过程，直到预测出 <code>&lt;EOS&gt;</code>。</p>
<p>Transformer 虽然消除了 RNN
在序列位置之间的递归计算，使一次前向传播中的不同位置能够并行处理，但实际进行自回归生成时，不同输出
token 之间仍然存在 <span class="math inline">\(y_1\rightarrow
y_2\rightarrow y_3\rightarrow\cdots\rightarrow y_T\)</span>
这样的顺序依赖。前面介绍的 Q、K、V、Multi-Head Attention、FFN、Residual
和
LayerNorm，本质上都是为这个过程服务的内部机制——它们不断构造和更新隐藏表示，而
Decoder
最终始终在回答同一个问题：在当前源序列和已有目标序列的条件下，下一个
token 应该是什么。</p>
<h2 id="transformer-训练样例">Transformer 训练样例</h2>
<p>前面的过程描述的是一个已经训练完成的 Transformer 如何根据
<code>I love cats</code> 逐步生成
<code>我 爱 猫</code>。但模型在训练开始时并不知道英文和中文之间应该如何对应，Embedding、Attention
中的投影矩阵、FFN
以及最终输出层中的参数也还没有学到有意义的数值。因此接下来的问题是：给定大量已经知道正确翻译的平行语料，Transformer
如何通过训练逐渐学会前面的生成过程？这里使用同一个语言翻译样本来进行展示。</p>
<p>推理时，正确的中文翻译事先并不知道，因此 Decoder 必须从
<code>&lt;BOS&gt;</code> 开始，一个 token 一个 token
地预测后续内容；训练时则不同，目标序列本身就是已经给出的监督信号，因此模型不仅知道输入是
<code>I love cats</code>，也知道正确输出应该是
<code>我 爱 猫</code>。</p>
<p>这使得训练阶段不需要像实际生成时那样分别运行
<code>&lt;BOS&gt;</code>、<code>&lt;BOS&gt; 我</code>、<code>&lt;BOS&gt; 我 爱</code>
等多轮 Decoder，而是可以将正确目标序列右移一位后一次输入 Decoder，再通过
Causal Mask
限制每个位置能够读取的信息。这样一次前向传播就可以同时训练多个
next-token prediction，随后根据正确答案计算
Loss，并利用反向传播更新整个模型中的可学习参数。</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 65
  &#125;
&#125;&#125;%%

flowchart LR

    S[&quot;源序列&lt;br/&gt;I · love · cats&quot;]

    ENC[&quot;Encoder × 6&quot;]

    Z[&quot;Encoder 最终表示 Z&quot;]

    T[&quot;正确目标序列&lt;br/&gt;我 · 爱 · 猫 · &amp;lt;EOS&amp;gt;&quot;]

    SHIFT[&quot;右移一位&lt;br/&gt;&amp;lt;BOS&amp;gt; · 我 · 爱 · 猫&quot;]

    DEC[&quot;Decoder × 6&lt;br/&gt;Causal Mask&quot;]

    P[&quot;Linear + Softmax&lt;br/&gt;所有位置同时预测&quot;]

    Y[&quot;监督目标&lt;br/&gt;我 · 爱 · 猫 · &amp;lt;EOS&amp;gt;&quot;]

    L[&quot;Cross-Entropy Loss&quot;]

    U[&quot;反向传播&lt;br/&gt;更新模型参数&quot;]

    S --&gt; ENC
    ENC --&gt; Z

    T --&gt; SHIFT
    SHIFT --&gt; DEC
    Z --&gt; DEC

    DEC --&gt; P
    P --&gt; L
    Y --&gt; L

    L --&gt; U

    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef encoder fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef memory fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef decoder fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef output fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef update fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111;

    class S,T input;
    class ENC encoder;
    class Z memory;
    class SHIFT,DEC decoder;
    class P,Y,L output;
    class U update;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
<h3 id="训练样本">训练样本</h3>
<p>源序列一侧与推理阶段没有本质区别。<code>I</code>、<code>love</code>
和 <code>cats</code> 首先经过 Embedding 和 Positional
Encoding，再完整经过 6 层 Encoder，最终得到</p>
<p><span class="math display">\[
Z=[z_I,z_{\text{love}},z_{\text{cats}}]
\]</span></p>
<p>真正不同的是 Decoder 的输入。</p>
<p>目标序列原本是
<code>我 爱 猫</code>。为了让模型同时学习什么时候结束，还需要在末尾加入
<code>&lt;EOS&gt;</code>，变成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">我 爱 猫 &lt;EOS&gt;</span><br></pre></td></tr></table></figure>
<p>训练 Decoder 时，不会直接把这组 token
原样作为输入和预测目标，而是将目标序列整体向右错开一个位置，在最前面加入
<code>&lt;BOS&gt;</code>。于是 Decoder 输入变成：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;BOS&gt; 我 爱 猫</span><br></pre></td></tr></table></figure>
<p>对应的监督目标则是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">我 爱 猫 &lt;EOS&gt;</span><br></pre></td></tr></table></figure>
<p>四个位置因此分别对应四个 next-token prediction：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;BOS&gt;           → 我</span><br><span class="line">&lt;BOS&gt; 我        → 爱</span><br><span class="line">&lt;BOS&gt; 我 爱     → 猫</span><br><span class="line">&lt;BOS&gt; 我 爱 猫  → &lt;EOS&gt;</span><br></pre></td></tr></table></figure>
<p>这里有一个和推理阶段非常重要的区别：Decoder 输入里的
<code>我</code>、<code>爱</code> 和 <code>猫</code>
并不是当前模型刚刚预测出来的
token，而是直接来自训练数据中的正确目标序列。</p>
<p>即使模型此时第一个位置更倾向于预测
<code>你</code>，第二个位置接收到的历史仍然是训练数据中的正确 token
<code>我</code>，而不会变成模型自己刚刚预测错误的
<code>你</code>。这种在训练时直接使用正确目标序列作为历史输入的方式通常称为
Teacher
Forcing。它使模型可以稳定地学习：在正确的目标前缀已经给定时，下一个
token 应该是什么。</p>
<p>不过这又产生了一个新的问题。既然 <code>&lt;BOS&gt; 我 爱 猫</code>
在训练时一次全部进入
Decoder，那么如果没有额外限制，<code>&lt;BOS&gt;</code>
所在的位置就可能直接读取后面的 <code>我</code>、<code>爱</code> 和
<code>猫</code>，模型甚至可以利用未来答案预测当前位置，这显然与实际生成过程不一致。因此
Decoder 的 Self-Attention 还需要使用 Causal Mask。</p>
<p>对于四个位置，允许读取的信息分别是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;BOS&gt; 只能读取 &lt;BOS&gt;</span><br><span class="line"></span><br><span class="line">我     可以读取 &lt;BOS&gt;、我</span><br><span class="line"></span><br><span class="line">爱     可以读取 &lt;BOS&gt;、我、爱</span><br><span class="line"></span><br><span class="line">猫     可以读取 &lt;BOS&gt;、我、爱、猫</span><br></pre></td></tr></table></figure>
<p>对应的 attention mask 可以写成</p>
<p><span class="math display">\[
M=
\begin{bmatrix}
\checkmark &amp; \times &amp; \times &amp; \times \\
\checkmark &amp; \checkmark &amp; \times &amp; \times \\
\checkmark &amp; \checkmark &amp; \checkmark &amp; \times \\
\checkmark &amp; \checkmark &amp; \checkmark &amp; \checkmark
\end{bmatrix}
\]</span></p>
<p>实际计算时，被禁止访问的位置会在 Softmax
之前被赋予极小值，通常可以理解为设置成 <span
class="math inline">\(-\infty\)</span>。由于 <span
class="math inline">\(e^{-\infty}=0\)</span>，这些位置经过 Softmax
后得到的 attention 权重就会变成 0，因此不会参与 Value 的加权混合。</p>
<p>这样一来，虽然正确目标序列已经完整输入
Decoder，但每个位置实际上仍然只能利用当前及之前的目标
token，无法偷看后面的正确答案。</p>
<h3 id="前向传播">前向传播</h3>
<p>经过目标序列右移和 Causal Mask
之后，一次完整的训练前向传播就可以开始。</p>
<p>Encoder 首先将 <code>I love cats</code> 处理成固定的源序列表示</p>
<p><span class="math display">\[
Z=[z_I,z_{\text{love}},z_{\text{cats}}]
\]</span></p>
<p>与此同时，Decoder 一次接收：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">&lt;BOS&gt; 我 爱 猫</span><br></pre></td></tr></table></figure>
<p>四个 token 都经过 Embedding 和 Positional Encoding，形成</p>
<p><span class="math display">\[
Y^0\in\mathbb R^{4\times512}
\]</span></p>
<p>随后整个 <span class="math inline">\(Y^0\)</span> 一次进入 Decoder 第
1 层。</p>
<p>Masked Self-Attention 会同时计算四个位置之间允许存在的
attention，只不过 Causal Mask
会禁止当前位置访问未来位置；Cross-Attention 则让四个位置分别使用自己的
Query 读取同一个 Encoder 表示 <span
class="math inline">\(Z\)</span>；随后各个位置分别经过 FFN。</p>
<p>第 1 层结束之后仍然得到四个位置的表示</p>
<p><span class="math display">\[
Y^1\in\mathbb R^{4\times512}
\]</span></p>
<p>它们继续进入第 2 层，再依次经过 Masked Multi-Head
Self-Attention、Cross-Attention 和 FFN，一直到第 6 层，最终得到</p>
<p><span class="math display">\[
H=[h_1,h_2,h_3,h_4]\in\mathbb R^{4\times512}
\]</span></p>
<p>这里的四个隐藏表示分别对应 Decoder 输入中的
<code>&lt;BOS&gt;</code>、<code>我</code>、<code>爱</code> 和
<code>猫</code>。</p>
<p>随后四个位置都会经过同一个输出 Linear，将 512
维隐藏表示映射到目标词表，并通过 Softmax 得到整个词表上的概率分布。</p>
<p>假设当前模型仍处于训练早期，四个位置分别得到的概率可能还不高，但一次
Decoder 前向传播已经同时完成了四个预测：</p>
<p><span class="math display">\[
P(y_1\mid \texttt{&lt;BOS&gt;})
\]</span></p>
<p><span class="math display">\[
P(y_2\mid \texttt{&lt;BOS&gt; 我})
\]</span></p>
<p><span class="math display">\[
P(y_3\mid \texttt{&lt;BOS&gt; 我 爱})
\]</span></p>
<p><span class="math display">\[
P(y_4\mid \texttt{&lt;BOS&gt; 我 爱 猫})
\]</span></p>
<p>对应的正确答案则是</p>
<p><span class="math display">\[
y_1=\text{我},\qquad
y_2=\text{爱},\qquad
y_3=\text{猫},\qquad
y_4=\texttt{&lt;EOS&gt;}
\]</span></p>
<p>这也是 Transformer
训练阶段与实际自回归生成之间一个非常重要的区别。</p>
<p>推理时必须实际执行一步一步的生成，因为后一个输入必须等前一个 token
真正生成出来之后才能确定。训练时正确目标序列已经知道，因此可以一次输入
<code>&lt;BOS&gt; 我 爱 猫</code>，再依靠 Causal Mask
将每个位置能够使用的信息限制成对应的前缀。</p>
<p>所以自回归描述的是信息依赖关系必须从左向右，并不意味着训练时的四个位置也必须真的按照时间顺序一个一个计算。通过
Causal Mask，这种因果约束被直接编码进 Attention
矩阵，因此同一层中的不同目标位置仍然可以并行处理。</p>
<h3 id="反向传播">反向传播</h3>
<p>完成一次前向传播之后，模型已经在四个位置上分别给出了整个目标词表的概率分布。接下来需要将这些预测与正确目标进行比较，从而判断模型当前预测得有多差。</p>
<p>这条训练样本的正确目标是：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><span class="line">位置 1 → 我</span><br><span class="line">位置 2 → 爱</span><br><span class="line">位置 3 → 猫</span><br><span class="line">位置 4 → &lt;EOS&gt;</span><br></pre></td></tr></table></figure>
<p>假设模型给这四个正确 token 的概率分别为</p>
<p><span class="math display">\[
P(\text{我})=0.40,\qquad
P(\text{爱})=0.35,\qquad
P(\text{猫})=0.60,\qquad
P(\texttt{&lt;EOS&gt;})=0.55
\]</span></p>
<p>如果先使用普通的 Cross-Entropy 来理解，那么每个位置的 Loss
可以写成</p>
<p><span class="math display">\[
L_t=-\log P(y_t)
\]</span></p>
<p>四个位置的平均 Loss 为</p>
<p><span class="math display">\[
L=
-\frac{1}{4}
\left[
\log0.40+
\log0.35+
\log0.60+
\log0.55
\right]
\]</span></p>
<p>如果模型给正确 token 的概率很低，例如 <span
class="math inline">\(P(\text{爱})=0.01\)</span>，那么 <span
class="math inline">\(-\log0.01\)</span> 会产生较大的
Loss；如果模型已经非常确定正确答案，例如 <span
class="math inline">\(P(\text{爱})=0.95\)</span>，对应的 Loss
就会比较小。</p>
<p>因此 Cross-Entropy 实际上在要求模型不断提高正确 next token
的概率，同时降低错误 token 的概率。</p>
<p>实际训练时当然不会只使用 <code>I love cats → 我 爱 猫</code>
这一条样本，而是将大量不同长度的翻译样本组成
batch，对其中所有有效目标位置计算 Loss，再对这些 Loss
进行汇总。对于为了组成 batch 而补出的 <code>&lt;PAD&gt;</code>
位置，则通常会通过 padding mask 排除，不让它们参与有效预测和 Loss
计算。</p>
<p>得到 Loss 以后，训练还没有结束。Loss
只是衡量当前模型的预测误差，真正让模型发生变化的是随后进行的反向传播。反向传播首先从最终的
Loss 开始，根据链式法则向前计算各个参数对 Loss 的影响。例如某个
Attention head 中</p>
<p><span class="math display">\[
Q=XW^Q,\qquad
K=XW^K,\qquad
V=XW^V
\]</span></p>
<p>真正需要学习的是 <span class="math inline">\(W^Q\)</span>、<span
class="math inline">\(W^K\)</span> 和 <span
class="math inline">\(W^V\)</span>。Loss 经过 Attention
的计算路径反向传播后，可以得到</p>
<p><span class="math display">\[
\frac{\partial L}{\partial W^Q},
\qquad
\frac{\partial L}{\partial W^K},
\qquad
\frac{\partial L}{\partial W^V}
\]</span></p>
<p>这些梯度描述的是：如果稍微改变当前参数，最终 Loss
会向什么方向变化。同样，FFN 中的 <span
class="math inline">\(W_1\)</span>、<span
class="math inline">\(b_1\)</span>、<span
class="math inline">\(W_2\)</span>、<span
class="math inline">\(b_2\)</span>，Multi-Head Attention 最后的 <span
class="math inline">\(W^O\)</span>，以及 Embedding、LayerNorm 和最终输出
Linear 等可学习参数，也都会得到自己的梯度。训练并不是只更新
Decoder。预测误差最初产生在 Decoder 最后的词表概率上，但 Decoder 的
Cross-Attention 使用了 Encoder 最终表示 <span
class="math inline">\(Z\)</span>。因此梯度会继续沿着 Cross-Attention 向
Encoder 传播，于是 Encoder 六层中的 Self-Attention、FFN
和其他可学习参数也会一起得到梯度。</p>
<p>整个 Transformer
因而形成一条端到端的可微计算链，训练时再沿相反方向计算。最终得到各层参数的梯度。得到梯度以后，优化器再根据这些梯度更新参数。最简单的梯度下降可以表示为
<span
class="math inline">\(\theta_{\text{new}}=\theta_{\text{old}}-\eta\frac{\partial
L}{\partial\theta}\)</span>，其中 <span
class="math inline">\(\theta\)</span> 表示模型中的某个可学习参数，<span
class="math inline">\(\eta\)</span> 是 learning rate。原始 Transformer
实际使用 Adam 优化器，并配合特定的 learning rate
schedule，这些属于训练配置中的具体实现。</p>
<p>完成一次更新后，同一条样本如果再次输入模型，得到的
Attention、隐藏表示和最终词表概率通常都会发生轻微变化。模型不会被直接告诉某个
head 应该负责主语、某个 head 应该负责动词，或者 <span
class="math inline">\(W^Q\)</span>
应该变成什么数值。训练提供的监督只有最终目标：<code>I love cats</code>
应该预测 <code>我 爱 猫 &lt;EOS&gt;</code>。至于中间的 <span
class="math inline">\(W^Q\)</span>、<span
class="math inline">\(W^K\)</span>、<span
class="math inline">\(W^V\)</span> 应该如何变化，哪些位置应该形成更强的
Attention，不同 head 应该学习什么表示空间，都是模型在不断降低最终 Loss
的过程中通过梯度下降共同形成的。</p>
<p>经过大量不同的平行语料和大量参数更新之后，模型才逐渐从最初没有意义的参数状态变成前一章描述的已经训练好的
Transformer：Encoder 能够形成有用的上下文表示 <span
class="math inline">\(Z\)</span>，Decoder 的 Cross-Attention
能够根据当前目标前缀读取相关源语言信息，最终输出层也能够逐渐提高正确
next token 的概率。</p>
<h2 id="训练与实验结果">训练与实验结果</h2>
<p>在介绍完 Transformer 的模型结构以及 Self-Attention
的设计之后，论文进一步给出了模型实际使用的训练配置，并通过机器翻译、模型变体实验和英语成分句法分析验证
Transformer 的效果。</p>
<h3 id="训练配置">训练配置</h3>
<p><strong>训练数据与 Batching</strong></p>
<p>机器翻译实验主要使用 WMT 2014 English-German 和 English-French
两个数据集。</p>
<p>English-German 数据集包含大约 450 万对平行句子。论文使用 Byte-Pair
Encoding（BPE）对文本进行编码，并在源语言和目标语言之间共享一个约 37,000
个 token 的词表。</p>
<p>English-French 数据集规模更大，包含大约 3600 万对句子，使用约 32,000
个 word-piece 作为词表。</p>
<p>训练时，作者按照近似的序列长度将句子对组织到同一个 batch
中，从而减少不同长度序列进行 padding 带来的额外计算。每个 batch 大约包含
25,000 个 source token 和 25,000 个 target token。</p>
<table>
<colgroup>
<col style="width: 24%" />
<col style="width: 23%" />
<col style="width: 12%" />
<col style="width: 39%" />
</colgroup>
<thead>
<tr class="header">
<th>数据集</th>
<th>训练规模</th>
<th>Tokenization</th>
<th>词表大小</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>WMT 2014 English-German</td>
<td>约 4.5M sentence pairs</td>
<td>BPE</td>
<td>约 37K，共享 source-target vocabulary</td>
</tr>
<tr class="even">
<td>WMT 2014 English-French</td>
<td>约 36M sentence pairs</td>
<td>WordPiece</td>
<td>约 32K</td>
</tr>
</tbody>
</table>
<p><strong>硬件与训练时间</strong></p>
<p>所有模型都在一台包含 8 块 NVIDIA P100 GPU 的机器上训练。</p>
<p>Base Transformer 每个 training step 大约需要 0.4 秒，总共训练 100,000
steps，因此完整训练时间约为 12 小时。Big Transformer 每个 step 大约需要
1 秒，总共训练 300,000 steps，训练时间约为 3.5 天。</p>
<table>
<colgroup>
<col style="width: 26%" />
<col style="width: 13%" />
<col style="width: 15%" />
<col style="width: 23%" />
<col style="width: 21%" />
</colgroup>
<thead>
<tr class="header">
<th>Model</th>
<th>GPUs</th>
<th>Step Time</th>
<th>Training Steps</th>
<th>Training Time</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Transformer Base</td>
<td>8 × P100</td>
<td>约 0.4 s</td>
<td>100K</td>
<td>约 12 h</td>
</tr>
<tr class="even">
<td>Transformer Big</td>
<td>8 × P100</td>
<td>约 1.0 s</td>
<td>300K</td>
<td>约 3.5 days</td>
</tr>
</tbody>
</table>
<p><strong>Optimizer 与 Learning Rate</strong></p>
<p>论文使用 Adam Optimizer，并设置 <span
class="math inline">\(\beta_1=0.9\)</span>、<span
class="math inline">\(\beta_2=0.98\)</span>、<span
class="math inline">\(\epsilon=10^{-9}\)</span>。</p>
<p>Learning Rate 并不是保持固定，而是根据训练步数动态变化：</p>
<p><span
class="math inline">\(lrate=d_{\text{model}}^{-0.5}\cdot\min(\text{step_num}^{-0.5},\text{step_num}\cdot\text{warmup_steps}^{-1.5})\)</span></p>
<p>其中 <span
class="math inline">\(\text{warmup_steps}=4000\)</span>。</p>
<p>因此训练开始后的前 4000 steps 中，Learning Rate
会随着训练步数线性增加；完成 warmup 之后，则按照 step number 的 inverse
square root
逐渐下降。这样的设计使模型在训练初期使用较小的更新幅度，之后逐步增大学习率，再随着训练进行逐渐减小。</p>
<p><strong>Regularization</strong></p>
<p>论文主要使用 Dropout 和 Label Smoothing 进行正则化。</p>
<p>Residual Dropout
会作用在每个子层的输出上，然后再与子层输入进行残差相加和
LayerNorm。同时，Encoder 和 Decoder 底部的 Embedding 与 Positional
Encoding 相加之后也会使用 Dropout。Base Transformer 的 dropout rate
设置为 <span class="math inline">\(P_{\text{drop}}=0.1\)</span>。</p>
<p>训练时还使用了 <span class="math inline">\(\epsilon_{ls}=0.1\)</span>
的 Label Smoothing。与严格的 one-hot target 相比，Label Smoothing
会让模型对正确 token 的预测更加保守，因此论文观察到它会使 perplexity
略有变差，但能够提高 accuracy 和 BLEU。</p>
<h3 id="实验结果">实验结果</h3>
<h4 id="机器翻译">机器翻译</h4>
<p>论文首先在 WMT 2014 English-German 和 English-French
两个机器翻译任务上与此前的 RNN、CNN 以及 Ensemble 模型进行比较。</p>
<p>主要结果如下：</p>
<table>
<thead>
<tr class="header">
<th>Model</th>
<th style="text-align: right;">EN-DE BLEU</th>
<th style="text-align: right;">EN-FR BLEU</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>ByteNet</td>
<td style="text-align: right;">23.75</td>
<td style="text-align: right;">-</td>
</tr>
<tr class="even">
<td>GNMT + RL</td>
<td style="text-align: right;">24.60</td>
<td style="text-align: right;">39.92</td>
</tr>
<tr class="odd">
<td>ConvS2S</td>
<td style="text-align: right;">25.16</td>
<td style="text-align: right;">40.46</td>
</tr>
<tr class="even">
<td>MoE</td>
<td style="text-align: right;">26.03</td>
<td style="text-align: right;">40.56</td>
</tr>
<tr class="odd">
<td>GNMT + RL Ensemble</td>
<td style="text-align: right;">26.30</td>
<td style="text-align: right;">41.16</td>
</tr>
<tr class="even">
<td>ConvS2S Ensemble</td>
<td style="text-align: right;">26.36</td>
<td style="text-align: right;">41.29</td>
</tr>
<tr class="odd">
<td>Transformer Base</td>
<td style="text-align: right;">27.30</td>
<td style="text-align: right;">38.10</td>
</tr>
<tr class="even">
<td><strong>Transformer Big</strong></td>
<td style="text-align: right;"><strong>28.40</strong></td>
<td style="text-align: right;"><strong>41.80</strong></td>
</tr>
</tbody>
</table>
<p>在 English-German 任务上，Transformer Big 达到 28.4 BLEU，比此前包括
Ensemble 在内的最佳结果高出 2 BLEU 以上，取得新的 state-of-the-art。</p>
<p>值得注意的是，即使是规模较小的 Transformer Base，也已经达到 27.3
BLEU，高于表中此前发表的单模型和 Ensemble，同时训练成本明显更低。</p>
<p>English-French 上，Transformer Big 达到 41.8
BLEU，同样取得了当时非常有竞争力的结果。论文重点强调的不只是最终
BLEU，还包括达到这些结果所需要的训练成本：Transformer
能够利用高度并行的矩阵计算，在远少于此前许多 RNN 和 CNN
模型的训练成本下得到相当甚至更高的翻译质量。</p>
<p>推理时，Base Model 使用最后 5 个 checkpoint 的参数进行平均，这些
checkpoint 之间间隔 10 分钟；Big Model 则平均最后 20 个 checkpoint。</p>
<p>机器翻译使用 Beam Search 生成最终序列，beam size 设置为 4，并使用
length penalty <span
class="math inline">\(\alpha=0.6\)</span>。最大输出长度被限制为输入长度加
50，但如果提前生成 <code>&lt;EOS&gt;</code>，则会直接结束生成。</p>
<h4 id="模型变体与消融实验">模型变体与消融实验</h4>
<p>论文随后在 WMT 2014 English-German 的 development set 上修改
Transformer 的不同组成部分，观察模型结构和超参数变化对结果的影响。</p>
<p>Base Transformer 的主要配置为：</p>
<table>
<thead>
<tr class="header">
<th>参数</th>
<th>Base Transformer</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Encoder / Decoder Layers <span class="math inline">\(N\)</span></td>
<td>6</td>
</tr>
<tr class="even">
<td><span class="math inline">\(d_{\text{model}}\)</span></td>
<td>512</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(d_{\text{ff}}\)</span></td>
<td>2048</td>
</tr>
<tr class="even">
<td>Attention Heads <span class="math inline">\(h\)</span></td>
<td>8</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(d_k\)</span></td>
<td>64</td>
</tr>
<tr class="even">
<td><span class="math inline">\(d_v\)</span></td>
<td>64</td>
</tr>
<tr class="odd">
<td><span class="math inline">\(P_{\text{drop}}\)</span></td>
<td>0.1</td>
</tr>
<tr class="even">
<td><span class="math inline">\(\epsilon_{ls}\)</span></td>
<td>0.1</td>
</tr>
<tr class="odd">
<td>Training Steps</td>
<td>100K</td>
</tr>
<tr class="even">
<td>Parameters</td>
<td>约 65M</td>
</tr>
</tbody>
</table>
<p>作者首先改变 Attention Head 的数量，同时调整每个 Head 的 <span
class="math inline">\(d_k\)</span> 和 <span
class="math inline">\(d_v\)</span>，使整体计算量大致保持不变。</p>
<p>实验发现，Single-Head Attention 的 BLEU 比最佳设置低约
0.9，说明使用多个 Attention Head 确实能够提升模型质量；但 Head
数量也不是越多越好，当 Head 继续增加时，性能同样会有所下降。</p>
<p>论文还单独减小了 Attention Key 的维度 <span
class="math inline">\(d_k\)</span>，结果发现较小的 <span
class="math inline">\(d_k\)</span> 会降低模型性能。作者据此认为，判断
Query 与 Key 之间的 compatibility 并不是一个非常简单的问题，更大的 Key
Representation 能够提供更充分的匹配信息。</p>
<p>模型规模实验则表现出比较明确的趋势：增加 Transformer Layer 数量、提高
<span class="math inline">\(d_{\text{model}}\)</span> 或增大 FFN
中间层维度 <span
class="math inline">\(d_{\text{ff}}\)</span>，通常都能够继续改善结果，但同时也会明显增加参数量。</p>
<p>Dropout 的实验也说明正则化对于 Transformer 的训练十分重要。取消
Dropout 后模型更容易出现过拟合，而适当使用 Dropout 可以明显改善
development set 上的表现。</p>
<p>Positional Encoding 方面，作者还将原本的 sinusoidal positional
encoding 替换为 learned positional
embedding。两种方法最终得到的结果几乎相同，说明 Transformer
的性能并不强依赖正弦位置编码本身。论文最终仍然选择 sinusoidal positional
encoding，主要是因为作者认为它可能更容易泛化到训练阶段没有出现过的更长序列。</p>
<p>Big Transformer 则进一步扩大模型规模，主要配置为 <span
class="math inline">\(N=6\)</span>、<span
class="math inline">\(d_{\text{model}}=1024\)</span>、<span
class="math inline">\(d_{\text{ff}}=4096\)</span>、<span
class="math inline">\(h=16\)</span>，参数量增加到约 213M，并训练 300K
steps。</p>
<p>整体来看，这组模型变体实验说明 Multi-Head Attention、足够大的
Attention Representation、模型容量以及 Dropout 都会影响最终性能，而
learned positional embedding 与 sinusoidal positional encoding
的结果则基本相同。</p>
<h4 id="english-constituency-parsing">English Constituency Parsing</h4>
<p>为了验证 Transformer 是否只能用于机器翻译，论文进一步将模型应用到了
English Constituency Parsing，也就是英语成分句法分析任务。</p>
<p>这一任务与机器翻译存在明显差异：输出需要满足较强的句法结构约束，而且生成的输出序列通常比输入序列明显更长。因此，这个实验主要用于检验
Transformer 能否泛化到具有不同输出结构的 sequence transduction
task。</p>
<p>作者使用 Penn Treebank 中 Wall Street
Journal（WSJ）部分的数据进行实验，其中只有大约 40K
条训练句子。同时还进行了 semi-supervised
实验，引入更大规模的额外语料。</p>
<p>这里使用的是一个 4-layer Transformer，并设置 <span
class="math inline">\(d_{\text{model}}=1024\)</span>。</p>
<p>主要实验结果如下：</p>
<table>
<colgroup>
<col style="width: 41%" />
<col style="width: 44%" />
<col style="width: 14%" />
</colgroup>
<thead>
<tr class="header">
<th>Parser</th>
<th>Training</th>
<th style="text-align: right;">WSJ 23 F1</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Vinyals &amp; Kaiser et al.</td>
<td>WSJ only, discriminative</td>
<td style="text-align: right;">88.3</td>
</tr>
<tr class="even">
<td>Petrov et al.</td>
<td>WSJ only, discriminative</td>
<td style="text-align: right;">90.4</td>
</tr>
<tr class="odd">
<td>Zhu et al.</td>
<td>WSJ only, discriminative</td>
<td style="text-align: right;">90.4</td>
</tr>
<tr class="even">
<td>Dyer et al.</td>
<td>WSJ only, discriminative</td>
<td style="text-align: right;"><strong>91.7</strong></td>
</tr>
<tr class="odd">
<td><strong>Transformer (4 layers)</strong></td>
<td><strong>WSJ only, discriminative</strong></td>
<td style="text-align: right;"><strong>91.3</strong></td>
</tr>
<tr class="even">
<td>Zhu et al.</td>
<td>semi-supervised</td>
<td style="text-align: right;">91.3</td>
</tr>
<tr class="odd">
<td>Huang &amp; Harper</td>
<td>semi-supervised</td>
<td style="text-align: right;">91.3</td>
</tr>
<tr class="even">
<td>McClosky et al.</td>
<td>semi-supervised</td>
<td style="text-align: right;">92.1</td>
</tr>
<tr class="odd">
<td>Vinyals &amp; Kaiser et al.</td>
<td>semi-supervised</td>
<td style="text-align: right;">92.1</td>
</tr>
<tr class="even">
<td><strong>Transformer (4 layers)</strong></td>
<td><strong>semi-supervised</strong></td>
<td style="text-align: right;"><strong>92.7</strong></td>
</tr>
<tr class="odd">
<td>Luong et al.</td>
<td>multi-task</td>
<td style="text-align: right;">93.0</td>
</tr>
<tr class="even">
<td>Dyer et al.</td>
<td>generative</td>
<td style="text-align: right;">93.3</td>
</tr>
</tbody>
</table>
<p>只使用大约 40K 条 WSJ 训练句子时，Transformer 已经取得 91.3
F1。虽然略低于 Dyer et al. 的 91.7，但已经超过包括 BerkeleyParser
在内的大多数此前方法。</p>
<p>在 semi-supervised 设置下，Transformer 进一步达到 92.7
F1，只低于表中的 multi-task 和 generative 方法。</p>
<p>这一实验中作者并没有针对 constituency parsing 对 Transformer
结构进行大量特殊设计，整体参数基本沿用机器翻译模型，只对少量
dropout、learning rate 和 beam size
等配置进行了选择。推理阶段最大输出长度被增加到输入长度加 300，beam size
设置为 21，length penalty 设置为 <span
class="math inline">\(\alpha=0.3\)</span>。</p>
<p>因此，论文认为这一结果说明 Transformer
并不局限于机器翻译。即使面对输出具有严格结构约束、输出长度明显不同，并且训练数据规模较小的任务，基于
Self-Attention 的架构仍然能够取得具有竞争力的结果。</p>
<p>整体来看，实验结果验证了论文前面对 Transformer
的主要论证：相比此前基于 recurrence 或 convolution 的 sequence
transduction model，Transformer
不仅能够取得更好的机器翻译质量，还能够利用更强的并行性显著降低训练时间；与此同时，在
English Constituency Parsing
上的结果也说明这一架构能够泛化到机器翻译之外的其他序列转换任务。</p>
<h2 id="总结">总结</h2>
<p>Transformer 是第一个完全基于注意力机制的序列模型，用 Multi-Head
Self-Attention 替代了 encoder-decoder 架构中最常用的循环层。在 WMT 2014
英译德、英译法两个翻译任务上，Transformer
都取得了当时的最优结果，且训练速度远快于以往基于循环或卷积结构的模型。论文还通过英语成分句法分析任务的实验，验证了
Transformer
具备良好的任务泛化能力，即便在小数据量场景下也能取得有竞争力的结果，说明这一架构不局限于机器翻译。</p>
<p>作者在结论部分提到几个计划推进的方向：一是将 Transformer
拓展到文本以外的输入输出模态，如图像、音频、视频等；二是研究局部的、受限的注意力机制，以高效处理超长序列的输入输出；三是探索让生成过程更少依赖串行步骤的方法。这几个方向后来都在深度学习的发展中得到了印证：ViT
把 attention
用到了图像上，各类高效注意力针对长序列做了优化，扩散模型等非自回归生成方式减少串行依赖。</p>
]]></content>
      <categories>
        <category>论文研析</category>
      </categories>
      <tags>
        <tag>经典论文</tag>
        <tag>人工智能基础</tag>
      </tags>
  </entry>
  <entry>
    <title>一卷新书入浅秋：2026-08</title>
    <url>/zh-blog/2026/08/01/%E4%B8%80%E5%8D%B7%E6%96%B0%E4%B9%A6%E5%85%A5%E6%B5%85%E7%A7%8B%EF%BC%9A2026-08/</url>
    <content><![CDATA[<p>八月初启，暑色未收，秋意已在风里。许多新的问题也像尚未翻开的书页一样，安静地等在前面。此刻并不急着知道它们最终会通向哪里，只愿在一次次阅读、推演与尝试之间，渐渐看清一些东西，也渐渐发现更多值得追问的方向。山长水阔，且从这一卷开始。</p>
<span id="more"></span>
<h2
id="navila从语言化导航决策到腿式控制的双层架构">NaVILA：从语言化导航决策到腿式控制的双层架构</h2>
<p>论文链接：</p>
<p><a href="https://arxiv.org/abs/2412.04453">NaVILA: Legged Robot
Vision-Language-Action Model for Navigation</a></p>
<h3 id="研究背景">研究背景</h3>
<p>视觉语言模型已经具备较强的场景理解和语言推理能力，但将这种能力直接用于腿式机器人仍然存在一个问题：高层导航关注目标、空间关系和长程决策，而机器人实际执行需要高频处理关节控制、动力学、地形和障碍，并且底层动作还与具体机器人本体强相关。直接从视觉和语言预测关节动作，不仅需要跨越语义推理与低层控制之间的表示差异，也使模型训练依赖大量机器人特定的控制数据。</p>
<p>NaVILA 因此采用双层架构。High-level VLA
根据自然语言指令和视觉观测进行导航推理，输出
<code>Move forward 75 cm</code>、<code>Turn right 30 degrees</code>
等带有空间信息的中层语言动作；Low-level Locomotion Policy
再将其转化为具体机器人的运动控制。这样既可以让高层模型利用仿真
VLN、人类视频以及通用视觉语言数据等更广泛的数据来源，也使中层动作摆脱具体机器人关节空间的限制，从而让同一个
High-level VLA
能够复用于不同机器人，而只需针对不同本体训练对应的低层控制策略。下图展现了论文提出的双层架构。</p>
<pre><code class="highlight mermaid">flowchart TB

    subgraph H[&quot; &quot;]
        direction TB

        HT[&quot;`High-level VLA
高层视觉语言导航`&quot;]

        I[&quot;自然语言指令&quot;]
        HIS[&quot;历史视觉观测&quot;]
        CUR[&quot;当前视觉观测&quot;]

        VLA[&quot;`VLA
视觉语言理解与导航决策`&quot;]

        A[&quot;`中层语言动作
Move forward 75 cm
Turn right 30°`&quot;]

        I --&gt; VLA
        HIS --&gt; VLA
        CUR --&gt; VLA
        VLA --&gt; A
    end

    P[&quot;`Parser
语言动作 → 速度命令`&quot;]

    subgraph L[&quot; &quot;]
        direction TB

        LT[&quot;`Low-level Locomotion
低层运动控制`&quot;]

        C[&quot;`速度命令
Linear / Angular Velocity`&quot;]

        PRO[&quot;`本体感知历史
关节位置 · 关节速度 · IMU`&quot;]

        LIDAR[&quot;`LiDAR Height Map
局部地形与障碍`&quot;]

        ACT[&quot;Locomotion Policy&quot;]

        J[&quot;`关节目标位置
Joint Targets`&quot;]

        R[&quot;`腿式机器人
Go2 / H1 / T1`&quot;]

        C --&gt; ACT
        PRO --&gt; ACT
        LIDAR --&gt; ACT
        ACT --&gt; J
        J --&gt; R
    end

    A --&gt; P
    P --&gt; C

    R -. &quot;新的视觉观测&quot; .-&gt; CUR
    R -. &quot;机器人状态&quot; .-&gt; PRO

    %% 以下不可见连线仅用于给标题预留空间
    HT ~~~ I
    HT ~~~ HIS
    HT ~~~ CUR

    LT ~~~ C
    LT ~~~ PRO
    LT ~~~ LIDAR

    classDef title fill:transparent,stroke:transparent,color:#111111,font-size:20px,font-weight:bold;
    classDef instruction fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef observation fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef high fill:#CFE3FF,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;
    classDef interface fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef sensor fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef low fill:#DDF3E4,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;
    classDef robot fill:#F0E2F5,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;

    class HT,LT title;
    class I instruction;
    class HIS,CUR observation;
    class VLA high;
    class A,P,C interface;
    class PRO,LIDAR sensor;
    class ACT low;
    class J,R robot;

    style H fill:#F7FAFF,stroke:#111111,stroke-width:3px;
    style L fill:#F7FCF8,stroke:#111111,stroke-width:3px;

    linkStyle default stroke:#111111,stroke-width:3px;
    linkStyle 13,14,15,16,17,18 stroke:transparent,stroke-width:0px;
</code></pre>
<h3 id="high-level-vla">High-level VLA</h3>
<p>NaVILA 的 High-level VLA
根据历史观测、当前视图和语言指令生成中层导航动作，而不直接预测低层关节控制。该模块以
VILA 为 backbone，并通过导航专用 prompt 和多源 SFT
数据进行适配。下面展示的是第一层模型的训练和推理流程。</p>
<pre><code class="highlight mermaid">flowchart TB

    subgraph TRAIN[&quot;　&quot;]
        direction TB

        TRAIN_TITLE[&quot;高层 VLA 训练&quot;]

        VILA[&quot;预训练 VILA&quot;]

        HV[&quot;`人类视频数据
（YouTube 导览视频）`&quot;]
        SIM[&quot;`仿真导航数据
（R2R-CE / RxR-CE）`&quot;]
        AUX[&quot;`辅助导航数据
（轨迹摘要 / ScanQA）`&quot;]
        VQA[&quot;通用 VQA 数据&quot;]

        TRAIN_TITLE ~~~ VILA

        HV --&gt; BLEND[&quot;SFT 数据混合&quot;]
        SIM --&gt; BLEND
        AUX --&gt; BLEND
        VQA --&gt; BLEND

        VILA --&gt; SFT[&quot;`监督微调
（SFT）`&quot;]
        BLEND --&gt; SFT

        SFT --&gt; NAVILA[&quot;`NaVILA
高层 VLA`&quot;]
    end

    subgraph INFER[&quot;　&quot;]
        direction TB

        INFER_TITLE[&quot;高层 VLA 推理&quot;]

        HIST[&quot;历史视图&quot;]
        CURR[&quot;当前视图&quot;]
        INST[&quot;语言指令&quot;]

        INFER_TITLE ~~~ HIST

        HIST --&gt; PROMPT[&quot;导航提示词&quot;]
        CURR --&gt; PROMPT
        INST --&gt; PROMPT

        PROMPT --&gt; MODEL[&quot;`NaVILA VLA
视觉编码器 + 投影层 + LLM`&quot;]

        MODEL --&gt; ACTION[&quot;中层动作指令&quot;]

        ACTION --&gt; FORWARD[&quot;前进 d 厘米&quot;]
        ACTION --&gt; TURN[&quot;左转 / 右转 θ°&quot;]
        ACTION --&gt; STOP[&quot;停止&quot;]
    end

    NAVILA -. &quot;训练完成&quot; .-&gt; MODEL

    classDef sectionTitle fill:#172554,stroke:#000000,stroke-width:4px,color:#FFFFFF,font-size:23px,font-weight:bold
    classDef pretrained fill:#9DD6FF,stroke:#000000,stroke-width:3px,color:#102A43,font-size:18px,font-weight:bold
    classDef human fill:#FFBBD3,stroke:#000000,stroke-width:3px,color:#45152A,font-size:18px,font-weight:bold
    classDef simulation fill:#A8EBC5,stroke:#000000,stroke-width:3px,color:#123B27,font-size:18px,font-weight:bold
    classDef auxiliary fill:#FFD591,stroke:#000000,stroke-width:3px,color:#4A3010,font-size:18px,font-weight:bold
    classDef vqa fill:#C9BAFF,stroke:#000000,stroke-width:3px,color:#2D2057,font-size:18px,font-weight:bold
    classDef process fill:#FFE76F,stroke:#000000,stroke-width:3px,color:#3D3400,font-size:18px,font-weight:bold
    classDef trained fill:#FF8C8C,stroke:#000000,stroke-width:4px,color:#451010,font-size:19px,font-weight:bold
    classDef input fill:#8DE1DB,stroke:#000000,stroke-width:3px,color:#103D3A,font-size:18px,font-weight:bold
    classDef prompt fill:#FFBD73,stroke:#000000,stroke-width:3px,color:#492A08,font-size:18px,font-weight:bold
    classDef model fill:#AC99FF,stroke:#000000,stroke-width:4px,color:#21164D,font-size:19px,font-weight:bold
    classDef action fill:#76D89E,stroke:#000000,stroke-width:4px,color:#0D3820,font-size:18px,font-weight:bold
    classDef command fill:#C5F2D7,stroke:#000000,stroke-width:3px,color:#123B27,font-size:17px,font-weight:bold

    class TRAIN_TITLE,INFER_TITLE sectionTitle
    class VILA pretrained
    class HV human
    class SIM simulation
    class AUX auxiliary
    class VQA vqa
    class BLEND,SFT process
    class NAVILA trained
    class HIST,CURR,INST input
    class PROMPT prompt
    class MODEL model
    class ACTION action
    class FORWARD,TURN,STOP command

    style TRAIN fill:#EFF7FF,stroke:#000000,stroke-width:4px,color:#111827
    style INFER fill:#FFF5EA,stroke:#000000,stroke-width:4px,color:#111827

    linkStyle default stroke:#000000,stroke-width:3px,color:#111111
</code></pre>
<h4 id="vila">VILA</h4>
<p>NaVILA 的 High-level VLA 建立在 VILA 之上。VILA 是一个 image-based
Vision-Language Model，主要由 Vision Encoder、Projector 和 LLM
三部分组成：图像首先被编码为 visual tokens，经降采样和 Projector
映射到语言空间，再与文本 token 一同输入 LLM 进行自回归生成。VILA
具有较强的 multi-image reasoning 能力，因此适合处理 VLN
中连续视觉观测之间的关系。</p>
<p>对于视频输入，VILA 并不使用专门的 video
encoder，而是将视频均匀采样为多帧图像后共同输入模型。但这种方式没有区分历史观测与当前观测的不同作用，因此
NaVILA 在此基础上进一步设计了 Navigation Prompt。</p>
<h4 id="navigation-prompt">Navigation Prompt</h4>
<p>原始 VILA
对视频帧进行均匀采样，但没有区分不同时间帧在导航中的作用。NaVILA
将视觉观测划分为 History Views 和 Current
View：当前帧用于判断机器人此刻的动作，而历史帧作为记忆信息，帮助模型追踪已经经过的位置和任务进度。具体地，模型始终保留最新帧作为当前观测，并从此前的历史帧中均匀采样，同时确保第一帧被保留。</p>
<p>为了让 LLM 显式区分二者，NaVILA 在 prompt 中使用
<code>"a video of historical observations:"</code> 和
<code>"current observation:"</code> 等文本提示，而没有额外引入特殊
token。随后将历史观测、当前观测与导航指令组合成完整的 Navigation
Prompt，引导模型输出带有距离或角度信息的中层动作，如 forward d cm、turn
left <span class="math inline">\(\theta^\circ\)</span> 或 stop。</p>
<h4 id="human-video">Human Video</h4>
<p>NaVILA 利用真实人类视频扩展连续导航训练数据。以约 2K 个 YouTube
第一视角 touring videos 为基础，作者首先通过 entropy-based sampling
提取约 20K 条具有代表性的 trajectories，再使用 MASt3R 估计 metric camera
pose，从人的相机运动中恢复对应的 step-wise navigation
actions。这使原本没有机器人动作标签的人类视频能够被转换为连续导航监督。</p>
<p>与此同时，作者利用 VILA 进行 trajectory captioning，再由 GPT-4o
rephrase，为每条 trajectory
生成对应的自然语言导航指令。最终，人类视频被转换为可用于 VLA 训练的
trajectory–instruction–action 数据，其中 action
采用距离和角度等中层空间表示，而不是机器人特定的关节动作。</p>
<pre><code class="highlight mermaid">flowchart LR

    VIDEO[&quot;`YouTube 视频
2K 第一视角导览视频`&quot;]
    SAMPLE[&quot;`基于熵的
轨迹采样`&quot;]
    TRAJ[&quot;`2 万条轨迹`&quot;]

    POSE[&quot;`度量位姿估计
MASt3R`&quot;]
    ACTION[&quot;`逐步动作
前进 d 厘米 / 转向 θ°`&quot;]

    CAPTION[&quot;`VILA
视频描述生成`&quot;]
    REPHRASE[&quot;`GPT-4o
指令改写`&quot;]
    INST[&quot;`导航指令`&quot;]

    VIDEO --&gt; SAMPLE
    SAMPLE --&gt; TRAJ

    TRAJ --&gt; POSE
    POSE --&gt; ACTION

    TRAJ --&gt; CAPTION
    CAPTION --&gt; REPHRASE
    REPHRASE --&gt; INST

    classDef source fill:#9DD6FF,stroke:#000000,stroke-width:4px,color:#102A43,font-size:19px,font-weight:bold
    classDef sampling fill:#FFD591,stroke:#000000,stroke-width:4px,color:#4A3010,font-size:19px,font-weight:bold
    classDef trajectory fill:#FF9F9F,stroke:#000000,stroke-width:5px,color:#451010,font-size:20px,font-weight:bold
    classDef geometry fill:#C9BAFF,stroke:#000000,stroke-width:4px,color:#2D2057,font-size:18px,font-weight:bold
    classDef action fill:#86D5A8,stroke:#000000,stroke-width:4px,color:#0D3820,font-size:18px,font-weight:bold
    classDef caption fill:#B8E1FF,stroke:#000000,stroke-width:4px,color:#102A43,font-size:18px,font-weight:bold
    classDef rephrase fill:#FFBBD3,stroke:#000000,stroke-width:4px,color:#45152A,font-size:18px,font-weight:bold
    classDef instruction fill:#A8EBC5,stroke:#000000,stroke-width:5px,color:#123B27,font-size:19px,font-weight:bold

    class VIDEO source
    class SAMPLE sampling
    class TRAJ trajectory
    class POSE geometry
    class ACTION action
    class CAPTION caption
    class REPHRASE rephrase
    class INST instruction

    linkStyle default stroke:#000000,stroke-width:3.5px,color:#111111
</code></pre>
<h4 id="blend-dataset">Blend Dataset</h4>
<p>仅依赖导航动作数据对 VLM
进行微调，容易使模型过度拟合特定的动作模式，同时削弱预训练 VLM
原有的视觉理解与通用知识。为了在学习导航能力的同时保持模型的泛化能力，NaVILA
构建了一个多来源的 SFT Dataset
Blend。训练数据主要从四个方面组成：真实视频导航数据、仿真导航数据、辅助导航数据以及通用
VQA 数据。</p>
<table>
<colgroup>
<col style="width: 16%" />
<col style="width: 33%" />
<col style="width: 49%" />
</colgroup>
<thead>
<tr class="header">
<th style="text-align: center;">数据类型</th>
<th style="text-align: center;">主要数据</th>
<th style="text-align: center;">主要作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">Real Navigation</td>
<td style="text-align: center;">Human Touring Videos</td>
<td style="text-align: center;">引入真实世界视觉与人类运动轨迹，提高
sim-to-real 泛化</td>
</tr>
<tr class="even">
<td style="text-align: center;">Simulated Navigation</td>
<td style="text-align: center;">R2R-CE、RxR-CE</td>
<td style="text-align: center;">提供标准 VLN 的 step-wise action
supervision，学习核心导航行为</td>
</tr>
<tr class="odd">
<td style="text-align: center;">Auxiliary Navigation</td>
<td style="text-align: center;">EnvDrop、Trajectory
Summarization、ScanQA</td>
<td
style="text-align: center;">增强指令多样性、轨迹理解与空间场景理解</td>
</tr>
<tr class="even">
<td style="text-align: center;">General VQA</td>
<td style="text-align: center;">LLaVA-1.5、ShareGPT4V、Video-ChatGPT
等</td>
<td style="text-align: center;">保留预训练 VLM
的通用视觉语言理解能力</td>
</tr>
</tbody>
</table>
<pre><code class="highlight mermaid">flowchart TB

    HV[&quot;`人类视频
真实世界导航`&quot;]

    SIM[&quot;`R2R-CE / RxR-CE
导航动作`&quot;]

    AUX[&quot;`EnvDrop / 轨迹摘要 / ScanQA
导航与空间理解`&quot;]

    VQA[&quot;`通用 VQA
通用 VLM 能力`&quot;]

    BLEND[&quot;`SFT 数据集混合`&quot;]
    SFT[&quot;`监督微调
SFT`&quot;]
    VLA[&quot;`NaVILA
高层 VLA`&quot;]

    HV --&gt; BLEND
    SIM --&gt; BLEND
    AUX --&gt; BLEND
    VQA --&gt; BLEND

    BLEND --&gt; SFT
    SFT --&gt; VLA

    classDef human fill:#FFBBD3,stroke:#000000,stroke-width:4px,color:#45152A,font-size:19px,font-weight:bold
    classDef sim fill:#A8EBC5,stroke:#000000,stroke-width:4px,color:#123B27,font-size:19px,font-weight:bold
    classDef aux fill:#FFD591,stroke:#000000,stroke-width:4px,color:#4A3010,font-size:18px,font-weight:bold
    classDef vqa fill:#C9BAFF,stroke:#000000,stroke-width:4px,color:#2D2057,font-size:19px,font-weight:bold
    classDef blend fill:#9DD6FF,stroke:#000000,stroke-width:5px,color:#102A43,font-size:20px,font-weight:bold
    classDef sft fill:#FFE76F,stroke:#000000,stroke-width:5px,color:#3D3400,font-size:20px,font-weight:bold
    classDef vla fill:#FF8C8C,stroke:#000000,stroke-width:5px,color:#451010,font-size:21px,font-weight:bold

    class HV human
    class SIM sim
    class AUX aux
    class VQA vqa
    class BLEND blend
    class SFT sft
    class VLA vla

    linkStyle default stroke:#000000,stroke-width:3.5px,color:#111111
</code></pre>
<h4 id="sft">SFT</h4>
<p>NaVILA 从已经完成视觉语言语料预训练的 VILA stage-2 model
开始训练，并使用前述多来源 SFT Dataset Blend 对模型进行监督微调。对于
navigation-specific data，历史帧与当前帧按照前述采样策略组织，并与
Navigation Prompt
一同构成模型输入，使模型学习生成带有距离或角度信息的中层导航动作。</p>
<p>训练时，Vision Encoder、Connector 和 LLM 均保持可训练状态，整个 VLM
共同进行参数更新，并在混合数据集上训练一个
epoch。通过这一过程，原本面向通用视觉语言任务的 VILA
被进一步适配为能够根据视觉历史、当前观测和语言指令生成导航动作的 NaVILA
High-level VLA。</p>
<h3 id="low-level-locomotion">Low-level Locomotion</h3>
<p>NaVILA 的 High-level VLA
只负责生成带有空间信息的中层导航动作，并不直接控制机器人关节。Low-level
Locomotion 首先将这些语言动作转换为 velocity commands，再由 visual
locomotion policy 结合 proprioception 和 LiDAR-based height map
实时生成机器人关节目标位置。该策略通过 single-stage PPO
在仿真环境中训练，使机器人能够在跟踪高层运动指令的同时完成局部避障和稳定运动。下图展示了第二层模型的训练和推理过程。</p>
<pre><code class="highlight mermaid">flowchart TB

    subgraph EXEC[&quot;　&quot;]
        direction TB

        EXEC_TITLE[&quot;低层运动执行&quot;]

        ACTION[&quot;`中层语言动作
前进 d 厘米
左转 / 右转 θ°`&quot;]
        PARSER[&quot;动作解析器&quot;]
        CMD[&quot;速度指令&quot;]

        LIDAR[&quot;LiDAR 点云&quot;]
        HEIGHT[&quot;`2.5D
高度图`&quot;]
        PROP[&quot;`本体感知
关节位置与速度
姿态信息
历史动作`&quot;]

        POLICY[&quot;`视觉运动策略
Visual Locomotion Policy`&quot;]
        JOINT[&quot;`12 个目标
关节位置`&quot;]

        EXEC_TITLE ~~~ ACTION

        ACTION --&gt; PARSER --&gt; CMD
        LIDAR --&gt; HEIGHT
        CMD --&gt; POLICY
        HEIGHT --&gt; POLICY
        PROP --&gt; POLICY
        POLICY --&gt; JOINT
    end

    subgraph TRAIN[&quot;　&quot;]
        direction TB

        TRAIN_TITLE[&quot;运动策略训练&quot;]

        SIM[&quot;`Isaac Sim
/ Isaac Lab`&quot;]
        TERRAIN[&quot;`地形环境
+ 域随机化`&quot;]

        ACTOR[&quot;`Actor
可部署的传感器输入`&quot;]
        CRITIC[&quot;`Critic
特权信息`&quot;]

        PPO[&quot;`单阶段
PPO 训练`&quot;]

        TRAINED[&quot;`训练完成的
视觉运动策略`&quot;]

        TRAIN_TITLE ~~~ SIM

        SIM --&gt; TERRAIN
        TERRAIN --&gt; ACTOR
        TERRAIN --&gt; CRITIC

        ACTOR --&gt; PPO
        CRITIC --&gt; PPO

        PPO --&gt; TRAINED
    end

    TRAINED -. &quot;部署&quot; .-&gt; POLICY

    classDef sectionTitle fill:#172554,stroke:#000000,stroke-width:4px,color:#FFFFFF,font-size:23px,font-weight:bold
    classDef actionInput fill:#FFBBD3,stroke:#000000,stroke-width:4px,color:#45152A,font-size:18px,font-weight:bold
    classDef parser fill:#FFD591,stroke:#000000,stroke-width:4px,color:#4A3010,font-size:18px,font-weight:bold
    classDef command fill:#FFE76F,stroke:#000000,stroke-width:4px,color:#3D3400,font-size:18px,font-weight:bold
    classDef sensor fill:#9DD6FF,stroke:#000000,stroke-width:4px,color:#102A43,font-size:18px,font-weight:bold
    classDef perception fill:#C9BAFF,stroke:#000000,stroke-width:4px,color:#2D2057,font-size:18px,font-weight:bold
    classDef policy fill:#FF8C8C,stroke:#000000,stroke-width:5px,color:#451010,font-size:20px,font-weight:bold
    classDef output fill:#86D5A8,stroke:#000000,stroke-width:4px,color:#0D3820,font-size:18px,font-weight:bold
    classDef simulation fill:#A8EBC5,stroke:#000000,stroke-width:4px,color:#123B27,font-size:18px,font-weight:bold
    classDef training fill:#FFCF96,stroke:#000000,stroke-width:4px,color:#492A08,font-size:18px,font-weight:bold
    classDef critic fill:#DDD2FF,stroke:#000000,stroke-width:4px,color:#2D2057,font-size:18px,font-weight:bold
    classDef ppo fill:#FFE76F,stroke:#000000,stroke-width:5px,color:#3D3400,font-size:20px,font-weight:bold
    classDef trained fill:#FF9F9F,stroke:#000000,stroke-width:5px,color:#451010,font-size:19px,font-weight:bold

    class EXEC_TITLE,TRAIN_TITLE sectionTitle
    class ACTION actionInput
    class PARSER parser
    class CMD command
    class LIDAR,PROP sensor
    class HEIGHT perception
    class POLICY policy
    class JOINT output
    class SIM simulation
    class TERRAIN training
    class ACTOR training
    class CRITIC critic
    class PPO ppo
    class TRAINED trained

    style EXEC fill:#F4F8FF,stroke:#000000,stroke-width:4px
    style TRAIN fill:#FFF5EA,stroke:#000000,stroke-width:4px

    linkStyle default stroke:#000000,stroke-width:3.5px,color:#111111
</code></pre>
<h4 id="policy">Policy</h4>
<p>High-level VLA 输出的中层动作仍然是自然语言形式，例如
<code>Move forward 75 cm</code> 或
<code>Turn right 30 degrees</code>，因此不能直接作为 locomotion policy
的输入。NaVILA 首先使用 regular-expression parser
从语言动作中提取动作类型及其空间参数，再将其转换为低层策略可以跟踪的
velocity command。</p>
<p>具体而言，<code>forward</code>、<code>left</code>、<code>right</code>
和 <code>stop</code> 分别对应固定的线速度或角速度：<span
class="math inline">\(v_{\text{forward}}=0.5\,\mathrm{m/s}\)</span>、<span
class="math inline">\(\omega_{\text{left}}=\frac{\pi}{6}\,\mathrm{rad/s}\)</span>、<span
class="math inline">\(\omega_{\text{right}}=-\frac{\pi}{6}\,\mathrm{rad/s}\)</span>，以及
<span class="math inline">\(v_{\text{stop}}=0\)</span>。</p>
<p>VLA 输出的距离或旋转角度用于确定该速度命令的持续时间。例如，对于
<code>Move forward 75 cm</code>，Parser 提取出 <code>forward</code> 和
<code>75 cm</code>，并将其转换为 <span
class="math inline">\(v_x=0.5\,\mathrm{m/s}\)</span>，持续时间为 <span
class="math inline">\(T=\frac{0.75}{0.5}=1.5\,\mathrm{s}\)</span>。</p>
<p>因此，高层 VLA 决定机器人朝什么方向运动以及运动多远，而 Low-level
Locomotion Policy 不再处理语言语义，而是在每个控制 timestep 中根据给定的
velocity command 和机器人当前状态生成关节控制，即 <span
class="math inline">\(\pi(o_t,v_t^{\mathrm{cmd}})\rightarrow
q_t^d\)</span>，其中 <span
class="math inline">\(q_t^d\in\mathbb{R}^{12}\)</span> 表示 Unitree Go2
十二个关节的目标位置。</p>
<h4 id="局部避障">局部避障</h4>
<p>NaVILA 使用 LiDAR 获取机器人周围的几何信息，并将点云转换为 2.5D
Height Map 输入 Low-level Locomotion Policy。Policy 在跟踪 VLA 所给
velocity command
的同时，根据局部地形信息实时调整运动，从而处理障碍物与复杂地形。相比高层
VLA 的低频导航决策，Low-level Policy 高频运行，负责实时 locomotion 与
obstacle avoidance。</p>
<h4 id="强化学习">强化学习</h4>
<p>NaVILA 的 Low-level Locomotion Policy 在 Isaac Sim 中使用 PPO
进行强化学习训练。不同于 teacher-student distillation，作者采用
single-stage RL training，直接训练最终用于部署的 Policy。</p>
<p>训练采用 asymmetric Actor-Critic。Actor
只使用真实部署时可获得的信息，包括 velocity command、proprioception
history 和 LiDAR Height Map，并输出 12 个关节目标位置 <span
class="math inline">\(q_t^d\in\mathbb{R}^{12}\)</span>。Critic
仅在训练阶段使用，可以额外访问仿真中的 privileged
information，以更准确地估计 value function 并辅助 PPO
更新。训练完成后只保留 Actor。</p>
<p>Reward 主要约束 velocity tracking、身体稳定性、运动平滑性和能耗等
locomotion objectives。同时，Policy
在多种地形中训练，并对质量、摩擦、motor strength、system delay 等进行
domain randomization，增强 sim-to-real 鲁棒性。</p>
<h2
id="mobilevla-r1缝合语义推理与底层控制之间的鸿沟">MobileVLA-R1：缝合语义推理与底层控制之间的鸿沟</h2>
<p>论文链接：</p>
<p><a href="https://arxiv.org/abs/2511.17889">MobileVLA-R1: Reinforcing
Vision-Language-Action for Mobile Robots</a></p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 70
  &#125;
&#125;&#125;%%

flowchart LR

    I[&quot;自然语言指令&lt;br/&gt;Instruction&quot;]

    RGB[&quot;RGB 图像&quot;]
    D[&quot;深度图&lt;br/&gt;Depth&quot;]
    P[&quot;三维点云&lt;br/&gt;Point Cloud&quot;]

    M[&quot;MobileVLA-R1&lt;br/&gt;多模态视觉语言动作模型&quot;]

    C[&quot;结构化 CoT 推理&lt;br/&gt;理解目标 · 场景 · 下一步动作&quot;]

    A[&quot;机器人动作输出&lt;br/&gt;Vx · Vy · ωyaw · α&quot;]

    R[&quot;四足机器人&lt;br/&gt;执行动作&quot;]

    E[&quot;环境发生变化&lt;br/&gt;进入下一时间步&quot;]


    I --&gt; M

    RGB --&gt; M
    D --&gt; M
    P --&gt; M

    M --&gt; C
    C --&gt; A
    A --&gt; R
    R --&gt; E

    E -. &quot;重新感知&quot; .-&gt; RGB
    E -. &quot;重新感知&quot; .-&gt; D
    E -. &quot;重新感知&quot; .-&gt; P


    classDef instruction fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef rgb fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef depth fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef point fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;

    classDef model fill:#CFE3FF,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;
    classDef reasoning fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef action fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef robot fill:#D8F0E3,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;
    classDef environment fill:#F0E2F5,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;


    class I instruction;
    class RGB rgb;
    class D depth;
    class P point;

    class M model;
    class C reasoning;
    class A action;
    class R robot;
    class E environment;


    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 55,
    &quot;rankSpacing&quot;: 70
  &#125;
&#125;&#125;%%

flowchart LR

    D[&quot;原始具身数据&lt;br/&gt;R2R / RxR / QUARD&quot;]

    C[&quot;构建 MobileVLA-CoT&lt;br/&gt;导航级 / 回合级 / 步级&quot;]

    N[&quot;NaVILA&lt;br/&gt;预训练 VLA&quot;]

    A[&quot;扩展模型结构&lt;br/&gt;RGB + Depth + Point Cloud&lt;br/&gt;LLaMA3-8B&quot;]

    S[&quot;阶段一：冷启动训练&lt;br/&gt;CoT 监督微调 SFT&quot;]

    G[&quot;阶段二：强化学习&lt;br/&gt;GRPO&quot;]

    R[&quot;奖励设计&lt;br/&gt;Movement / Action / Format&quot;]

    M[&quot;MobileVLA-R1&lt;br/&gt;语义推理 → 连续控制&quot;]

    D --&gt; C

    N --&gt; A

    C --&gt; S
    A --&gt; S

    S --&gt; G
    R --&gt; G

    G --&gt; M


    classDef data fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef model fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef sft fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef reward fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef grpo fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef final fill:#DDF3E4,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;

    class D,C data;
    class N,A model;
    class S sft;
    class R reward;
    class G grpo;
    class M final;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<h3 id="研究背景-1">研究背景</h3>
<p>MobileVLA-R1
关注的是高层语义推理与机器人底层控制之间的鸿沟。对于“绕过箱子，走到水桶旁边”这样的指令，视觉语言模型可以理解目标和空间关系，但真正控制机器狗时，还需要把这种理解进一步落实为前进速度、转向角速度和具体动作。论文认为，直接从语言和视觉预测动作容易出现语义落地不稳定的问题，而隐藏的中间表示又缺少清晰、可解释的推理过程。</p>
<p>受到 DeepSeek-R1 利用 CoT 与 GRPO
强化推理能力的启发，作者希望把类似的 reasoning + reinforcement learning
路线引入具身控制，让机器人不是直接从语言跳到底层动作，而是先形成显式的结构化推理，再把推理结果转化为连续控制。</p>
<p>为此，论文主要做了三件事：构建多粒度的 MobileVLA-CoT 数据集，在
NaVILA 基础上扩展 RGB、Depth 和 Point Cloud 多模态感知，并采用 SFT +
GRPO 的两阶段训练，使模型逐步学会从语义推理落到底层控制。</p>
<h3 id="mobilevla-cot-数据集">MobileVLA-CoT 数据集</h3>
<p>MobileVLA-CoT 并不是从头采集的一套机器人数据，而是在 R2R、RxR 和
QUARD 三个已有数据集上补充 CoT 标注得到的。R2R 和 RxR 都属于 VLN
数据，核心是自然语言指令与对应的导航轨迹，其中 RxR
的指令更长，并进一步加入多语言和更细粒度的时序对齐；QUARD
则面向四足机器人控制，除了视觉和任务指令之外，还记录机器人执行过程中的状态与动作，因此能够提供连续速度、步态等更底层的控制监督。论文也正是利用前两者补充长程导航推理，利用
QUARD 补充机器人动作层面的推理。</p>
<p>作者使用 Gemini-2.5-Flash 对这些已有轨迹进行进一步标注。输入包括
RGB–Depth 观测、自然语言指令以及对应的 state-action history，Gemini
根据结构化 Prompt 在原有动作之前补充
<code>&lt;think&gt;...&lt;/think&gt;</code>
形式的推理，并把对应的导航动作或控制信号写入
<code>&lt;answer&gt;...&lt;/answer&gt;</code>，最终形成三种不同粒度的
MobileVLA-CoT。R2R 和 RxR 主要用于生成 MobileVLA-CoT-Nav，QUARD 则生成
MobileVLA-CoT-Episode 和 MobileVLA-CoT-Step；三者规模分别为 38K、18K 和
78K。原始数据集与生成数据集的关系如下所示。</p>
<pre><code class="highlight mermaid">flowchart LR

    R[&quot;R2R / RxR&lt;br/&gt;语言 + 导航轨迹&quot;]
    Q[&quot;QUARD&lt;br/&gt;视觉 + 指令 + 机器人控制&quot;]

    N[&quot;MobileVLA-CoT-Nav&lt;br/&gt;38K&quot;]
    E[&quot;MobileVLA-CoT-Episode&lt;br/&gt;18K&quot;]
    S[&quot;MobileVLA-CoT-Step&lt;br/&gt;78K&quot;]

    R --&gt; N
    Q --&gt; E
    Q --&gt; S

    classDef raw fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef nav fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef episode fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef step fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;

    class R,Q raw;
    class N nav;
    class E episode;
    class S step;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<p>三种 CoT 的区别主要在于观察任务的尺度不同。</p>
<ul>
<li>Episode-CoT
面向完整的一次机器人任务，更接近对整段轨迹的宏观总结，例如机器人如何接近目标、什么时候避开障碍、之后又如何重新调整方向并完成任务，因此主要描述的是整段执行过程中的高层策略。论文给出的
Episode 示例也会依次总结 command sequence、对环境变化的响应、整体
strategy 和最终结果。</li>
<li>Nav-CoT
更关注长程自然语言指令与当前导航进度之间的对应关系。例如指令是“离开卧室，进入浴室，然后停在马桶旁”，模型需要结合当前和之前看到的环境判断自己已经执行到了哪一部分：是否已经离开卧室、当前看到的门是不是浴室入口，以及接下来应该继续前进还是转向，最后给出
<code>move forward</code>、<code>turn left</code>
这类语义层面的导航动作。</li>
<li>Step-CoT
则进一步靠近底层控制。它面对的是某一个具体时间步，根据当前环境和任务解释为什么机器人此时应该这样运动，并直接输出
<code>x_vel_cmd</code>、<code>y_vel_cmd</code>、<code>yaw_vel_cmd</code>、gait、step
frequency 等控制参数。</li>
</ul>
<p>整体上来说，Episode-CoT偏向于宏观规划，Nav-CoT偏向于识别当前在指令的哪一步以及当前需要做的动作用自然语言怎么表述，Step-CoT偏向于怎么把自然语言的动作转化为控制机器人做相应动作的命令序列。</p>
<h3 id="mobilevla-r1-模型">MobileVLA-R1 模型</h3>
<p>MobileVLA-R1 的模型结构并不是从头设计，而是以 NaVILA
为初始化模型，在原有视觉语言导航能力的基础上进一步加入深度和三维点云信息。整体仍然沿用
LLaVA 风格的多模态架构：RGB、Depth 和 Point Cloud
分别经过对应的编码器提取特征，再通过 projection layer
映射到与语言模型兼容的表示空间，与自然语言指令一起送入 LLaMA3-8B。论文中
RGB 部分继承自 NaVILA，Depth 使用 DepthAnything V2，Point Cloud 则使用
Point Transformer v3。下面是整个模型的结构流程图。</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 65
  &#125;
&#125;&#125;%%

flowchart LR

    T[&quot;自然语言指令&quot;]
    RGB[&quot;RGB&quot;]
    D[&quot;Depth&quot;]
    P[&quot;Point Cloud&quot;]

    RE[&quot;RGB Encoder&quot;]
    DE[&quot;Depth Encoder&quot;]
    PE[&quot;Point Cloud Encoder&quot;]

    PROJ[&quot;Projection Layer&quot;]
    LLM[&quot;LLaMA3-8B&lt;br/&gt;+ LoRA&quot;]

    COT[&quot;CoT 推理&quot;]
    A[&quot;动作输出&lt;br/&gt;Vx · Vy · ωyaw · α&quot;]

    RGB --&gt; RE
    D --&gt; DE
    P --&gt; PE

    RE --&gt; PROJ
    DE --&gt; PROJ
    PE --&gt; PROJ

    PROJ --&gt; LLM
    T --&gt; LLM

    LLM --&gt; COT
    COT --&gt; A

    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef encoder fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef projector fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef llm fill:#FFF1C7,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;
    classDef cot fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef action fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;

    class T,RGB,D,P input;
    class RE,DE,PE encoder;
    class PROJ projector;
    class LLM llm;
    class COT cot;
    class A action;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<p>加入 Depth 和 Point Cloud 的目的，是在 RGB
语义信息之外补充更直接的空间几何信息。Depth
提供物体距离和前后层次，Point Cloud
则进一步描述三维空间结构，因此模型不仅需要判断“画面里有什么”，还能够利用距离和几何关系决定当前该向哪里运动。论文也通过后面的模态消融实验验证了
Depth 和 Point Encoder 都能继续提升导航结果。</p>
<p>模型最终在每个时间步接收当前的 RGB、Depth、Point Cloud
和自然语言指令，并预测机器人动作 <span class="math inline">\(a_t = [V_x,
V_y, \omega_{\text{yaw}},
\alpha]\)</span>。其中前三项分别表示前后、横向和转向速度，<span
class="math inline">\(\alpha\)</span>
则表示离散的高层动作。与普通直接预测动作的 VLA 不同，MobileVLA-R1
希望先通过 LLaMA3-8B 形成显式的 CoT
推理，再将结果落实到具体的机器人控制上。</p>
<p>参数训练上，作者没有重新更新整个 8B 模型，而是采用 LoRA
进行参数高效微调，主要作用在 projection 和 LLaMA3-8B 的 attention
layer，视觉编码器保持冻结。这样既保留 NaVILA
已有的视觉语言能力，又可以用较小的训练成本让模型适应新的多模态输入和 CoT
控制任务。</p>
<h3 id="sft-grpo-训练">SFT + GRPO 训练</h3>
<p>MobileVLA-R1 的训练分成两个阶段。作者受到 DeepSeek-R1
的启发，希望通过强化学习进一步提升模型的推理能力，但对于同时包含视觉、语言和机器人动作的多模态模型，直接进行端到端强化学习容易出现训练不稳定。因此作者先利用前面构造的
MobileVLA-CoT
进行监督微调，让模型先学会稳定地产生结构化推理和动作，再通过 GRPO
对推理与实际控制之间的对应关系进行强化。</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 50,
    &quot;rankSpacing&quot;: 65
  &#125;
&#125;&#125;%%

flowchart LR

    M[&quot;NaVILA 初始化&lt;br/&gt;MobileVLA-R1&quot;]

    E[&quot;Episode-CoT&lt;br/&gt;整段任务总结与策略&quot;]
    N[&quot;Nav-CoT&lt;br/&gt;长程导航推理&quot;]
    S[&quot;Step-CoT&lt;br/&gt;可执行动作命令&quot;]

    SFT1[&quot;SFT 第一阶段&lt;br/&gt;学习结构化推理&quot;]
    SFT2[&quot;SFT 第二阶段&lt;br/&gt;推理 → 可执行动作&quot;]

    R[&quot;GRPO 强化学习&lt;br/&gt;Movement + Action + Format Reward&quot;]

    F[&quot;最终模型&lt;br/&gt;GRPO 后的 MobileVLA-R1&quot;]

    M --&gt; SFT1
    E --&gt; SFT1
    N --&gt; SFT1

    SFT1 --&gt; SFT2
    S --&gt; SFT2

    SFT2 --&gt; R
    R --&gt; F

    classDef model fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef cot fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef sft fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef grpo fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef final fill:#DDF3E4,stroke:#111111,stroke-width:4px,color:#111111,font-weight:bold;

    class M model;
    class E,N,S cot;
    class SFT1,SFT2 sft;
    class R grpo;
    class F final;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<h4 id="sft-冷启动">SFT 冷启动</h4>
<p>SFT 又可以分成两个步骤。首先使用完整的 Episode-CoT 和 Nav-CoT
训练模型，使其学会按照
<code>&lt;think&gt;...&lt;/think&gt;&lt;answer&gt;...&lt;/answer&gt;</code>
的格式进行推理。前者提供整个任务尺度的执行策略，后者提供长程导航过程中当前进度和下一步动作的判断，因此这一阶段主要解决的是“模型应该怎样思考和表达决策”。</p>
<p>随后再加入约 10K 条 Step-CoT。与前两种数据不同，Step-CoT 的
<code>&lt;answer&gt;</code>
中已经包含可以执行的速度和动作命令，例如前进速度、横向速度和 yaw
角速度等，这些输出可以被确定性地解析成机器人控制信号。因此第二步实际上是在前面已经建立的语义推理基础上，进一步学习如何把“应该怎么做”落到具体控制量上。</p>
<p>从训练方式上看，SFT
和普通语言模型监督微调没有本质区别。给定视觉、语言和一条 Gemini
生成的标准 CoT，模型按照 teacher forcing 学习标准答案中的下一个
token。也就是说，SFT 告诉模型的是：</p>
<figure class="highlight txt"><table><tr><td class="code"><pre><span class="line">对于这个输入，希望你尽可能复现这一条</span><br><span class="line">&lt;think&gt; ... &lt;/think&gt;</span><br><span class="line">&lt;answer&gt; ... &lt;/answer&gt;</span><br></pre></td></tr></table></figure>
<p>这样可以很快建立稳定的输出格式和基本推理方式，但它仍然依赖 Gemini
给出的单条标准答案。如果某一种推理写法最终产生了更好的机器人动作，仅靠
SFT 本身并不会主动偏向这种结果，因此作者继续使用 GRPO 进行强化。</p>
<h4 id="grpo">GRPO</h4>
<p>GRPO 阶段不再为一个输入只提供一条固定的标准答案。对于同一个多模态输入
<span class="math inline">\(I=(x,i)\)</span>，当前模型一次采样得到 <span
class="math inline">\(N\)</span> 条不同的输出：</p>
<p><span class="math display">\[
o_1,o_2,\ldots,o_N
\]</span></p>
<p>论文训练时，每个样本生成 8 个 response，每个 optimization step 使用 5
个样本。Reward 有三个方面：</p>
<ul>
<li>Movement Reward比较预测速度方向和真实速度方向，使用 <span
class="math inline">\(V_x\)</span>、<span
class="math inline">\(V_y\)</span>、<span
class="math inline">\(V_{\text{yaw}}\)</span>
三个连续控制量的余弦相似度：</li>
</ul>
<p><span class="math display">\[
R_m=
\frac{
\mathbf{o}&#39;^{\top}\mathbf{o}&#39;^{*}
}{
\|\mathbf{o}&#39;\|_2\|\mathbf{o}&#39;^{*}\|_2
},
\qquad
\mathbf{o}&#39;=(V_x,V_y,V_{\text{yaw}})
\]</span></p>
<ul>
<li>Action Reward 用于监督离散的高层动作。如果模型预测的动作 <span
class="math inline">\(a\)</span> 与真实动作 <span
class="math inline">\(a^*\)</span> 一致，则 reward 为 1，否则为 0：</li>
</ul>
<p><span class="math display">\[
R_{\text{action}}
=
\begin{cases}
1, &amp; a=a^* \\
0, &amp; \text{otherwise}
\end{cases}
\]</span></p>
<ul>
<li>Format Reward
检查模型是否仍然保持：<code>&lt;think&gt;...&lt;/think&gt;&lt;answer&gt;...&lt;/answer&gt;</code>这样的结构化输出格式：</li>
</ul>
<p><span class="math display">\[
R_{\text{format}}
=
\begin{cases}
1, &amp; \text{output satisfies format} \\
0, &amp; \text{otherwise}
\end{cases}
\]</span></p>
<p>论文分别定义了三种 reward，但没有明确给出它们如何组合成最终 reward
<span class="math inline">\(r_i\)</span>，也没有报告三种 reward
的具体权重。消融实验表明，三个 Reward
在一起才能发挥出最好的效果。下面是对三种Reward的总结归纳：</p>
<table>
<colgroup>
<col style="width: 23%" />
<col style="width: 38%" />
<col style="width: 38%" />
</colgroup>
<thead>
<tr class="header">
<th>Reward</th>
<th>约束对象</th>
<th>希望模型学到什么</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Movement Reward</td>
<td><span class="math inline">\(V_x,V_y,V_{\text{yaw}}\)</span></td>
<td>运动方向与真实控制一致</td>
</tr>
<tr class="even">
<td>Action Reward</td>
<td>离散动作 <span class="math inline">\(a\)</span></td>
<td>高层动作选择正确</td>
</tr>
<tr class="odd">
<td>Format Reward</td>
<td><code>&lt;think&gt;&lt;answer&gt;</code></td>
<td>保持结构化、可解析的输出</td>
</tr>
</tbody>
</table>
<p>GRPO 的关键不只是计算 reward，而是在同一组 response
之间进行相对比较。</p>
<p>假设同一个输入生成的 <span class="math inline">\(N\)</span> 个
response 分别得到：</p>
<p><span class="math display">\[
r_1,r_2,\ldots,r_N
\]</span></p>
<p>论文计算第 <span class="math inline">\(i\)</span> 个 response 的
advantage：</p>
<p><span class="math display">\[
A_i
=
r_i-
\frac{1}{N}
\sum_{j=1}^{N}r_j
\]</span></p>
<p>也就是说，一个 response 是否值得强化，不只取决于自己的 reward
有多大，而取决于它是否高于同一组 response 的平均 reward。</p>
<p>高于平均水平的 response 得到正 advantage，在之后的 policy update
中提高出现概率；低于平均水平的 response 得到负
advantage，则降低出现概率。</p>
<p>如果只按照 advantage 更新，模型可能会因为某个 response 的 reward
较高而一次性大幅提高它的概率，因此 GRPO 沿用了 PPO 中的 clipping
机制，将概率比值限制在 <span class="math inline">\(1-\epsilon\)</span>
到 <span class="math inline">\(1+\epsilon\)</span> 附近，并通过
<code>min</code> 选择更加保守的目标，从而限制单次 policy update
的幅度。</p>
<p>这样一来，GRPO 的完整过程就是先让当前模型针对同一个输入生成多个
response，根据 Movement、Action 和 Format 三种 reward 对这些 response
进行评价，再通过组内比较得到 advantage，最后利用 <span
class="math inline">\(J_{\mathrm{GRPO}}\)</span> 提高较好 response
的生成概率、降低较差 response 的生成概率，同时通过 clipping 和 KL
regularization 控制更新幅度。GRPO的流程大致如下所示。</p>
<pre><code class="highlight mermaid">%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 60
  &#125;
&#125;&#125;%%

flowchart LR

    I[&quot;同一个输入&lt;br/&gt;Instruction + Observation&quot;]

    P[&quot;当前 Policy&quot;]

    O1[&quot;Response 1&quot;]
    O2[&quot;Response 2&quot;]
    O3[&quot;...&quot;]
    ON[&quot;Response N&quot;]

    R[&quot;计算 Reward&lt;br/&gt;Movement · Action · Format&quot;]

    G[&quot;组内比较&lt;br/&gt;计算相对 Advantage&quot;]

    U[&quot;GRPO 更新 Policy&quot;]

    REF[&quot;SFT Reference Model&lt;br/&gt;KL 约束&quot;]

    I --&gt; P

    P --&gt; O1
    P --&gt; O2
    P --&gt; O3
    P --&gt; ON

    O1 --&gt; R
    O2 --&gt; R
    O3 --&gt; R
    ON --&gt; R

    R --&gt; G
    G --&gt; U

    REF -. &quot;限制策略偏移&quot; .-&gt; U

    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef policy fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef response fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef reward fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef grpo fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;
    classDef ref fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111,font-weight:bold;

    class I input;
    class P policy;
    class O1,O2,O3,ON response;
    class R reward;
    class G,U grpo;
    class REF ref;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<h2 id="fsr-vln-融合几何与图像两条路线的快慢推理">FSR-VLN:
融合几何与图像两条路线的快慢推理</h2>
<p>论文链接：</p>
<p><a href="https://arxiv.org/abs/2509.13733">FSR-VLN: Fast and Slow
Reasoning for Vision-Language Navigation with Hierarchical Multi-modal
Scene Graph</a></p>
<pre><code class="highlight mermaid">flowchart LR

    M[&quot;HMSG&lt;br/&gt;预构建空间记忆&quot;]

    Q[&quot;用户指令&quot;] --&gt; L[&quot;LLM&lt;br/&gt;目标理解&quot;]
    L --&gt; F[&quot;Fast Matching&quot;]
    F --&gt; S[&quot;Slow Reasoning&quot;]

    S --&gt;|成功| O[&quot;Goal View / Object&lt;br/&gt;3D Position&quot;]
    S --&gt;|失配| R[&quot;Fallback Refinement&quot;]
    R --&gt; O

    M -. 支撑检索 .-&gt; F
    M -. 提供 best_view .-&gt; S
    M -. 提供关联关系 .-&gt; R

    classDef map fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;
    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef fast fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef verify fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef fallback fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef output fill:#F8D9C4,stroke:#111111,stroke-width:3px,color:#111111;

    class M map;
    class Q,L input;
    class F fast;
    class S verify;
    class R fallback;
    class O output;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<h3 id="研究背景-2">研究背景</h3>
<p>FSR-VLN
研究的是在离线场景图已经构建完成的条件下，如何根据用户指令找到目标物体。系统的输入是一条文本或语音指令，输出则是目标物体对应的视图节点以及它在场景中的三维坐标。也就是说，这篇论文主要关注的是建图完成之后的目标理解、检索和定位，而不是在线探索或实时建图。</p>
<p>现有方法大致可以分成两条路线。一类是 OK-Robot、HOVSG
这样的几何语义地图或 3D 场景图，把 CLIP、OWL-ViT
等视觉模型的特征存到体素或物体节点中，检索时直接计算指令文本和节点特征之间的相似度。这类方法有明确的三维几何表示，目标一旦匹配到物体节点，就比较容易得到空间位置，但对目标的理解主要依赖
embedding
匹配。另一类方法则把环境表示成由历史图像组成的拓扑图，再直接利用 VLM
或长上下文模型在图像序列中寻找目标，语义推理能力更强，但缺少显式的物体级三维几何表示。FSR-VLN
想做的就是把这两种表示结合起来：既保留场景图中的几何和物体信息，也保留原始图像供
VLM 做进一步判断，因此在原有的 Floor–Room–Object 结构中加入了 View
节点，形成后面的 HMSG。两条路线和本文脉络如下所示。</p>
<pre><code class="highlight mermaid">flowchart LR
    A[&quot;几何语义地图 / 3D 场景图&quot;]
    B[&quot;三维几何结构明确&lt;br/&gt;语义主要依赖特征匹配&quot;]

    C[&quot;图像拓扑图 + VLM&quot;]
    D[&quot;图像语义推理能力强&lt;br/&gt;缺少显式物体级三维结构&quot;]

    A --&gt; B
    C --&gt; D

    B --&gt; E[&quot;FSR-VLN&lt;br/&gt;融合几何与图像两条路线&quot;]
    D --&gt; E

    classDef geo fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef vision fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef fusion fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;

    class A,B geo;
    class C,D vision;
    class E fusion;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
<h3 id="hmsg">HMSG</h3>
<p>FSR-VLN 使用的是 Hierarchical Multi-modal Scene
Graph（HMSG）。它并不是从头设计一种新的地图表示，而是在 HOVSG 原有的
Floor–Room–Object 三层场景图中加入了一层 View，用 View
把原始图像和三维物体连接起来。整体关系可以理解为 Floor 下面包含
Room，每个 Room 下面同时包含 View 和 Object；View 与它能够观测到的
Object 之间建立可见关系，不同 View
之间则根据相对位姿建立拓扑连接。这样一来，Floor 和 Room
仍然负责组织空间层级，Object
保留具体物体的三维包围盒、点云和语义特征，而 View
则保留机器人历史观测到的图像、相机位姿、CLIP 特征以及 VLM
生成的文字描述。</p>
<p>HMSG 的底层建图基本沿用了 HOVSG。机器人先通过 RGBD 相机和 LiDAR
采集数据，由 FAST-LIVO2
得到带位姿的点云和图像，再依次完成楼层划分、房间边界划分和物体实例聚类，得到原来的
Floor–Room–Object 场景图。这部分并不是 FSR-VLN
的主要创新，论文真正新增的是如何把历史图像作为 View
节点接入这套结构。具体来说，每个 View 根据相机位姿和房间边界确定所属
Room，同时由 VLM 为图像生成一段文字描述。</p>
<p>在 View 接入之后，系统还会为每个 Object 找到所有能够看到它的
View，并计算物体在这些视图中的平均深度，选择平均深度最小的那个作为该物体的
best_view。这个 best_view 后面很重要，因为在线推理时 Slow Reasoning
会直接用它对应的原始图像来核实 CLIP 找到的物体是否正确。除此之外，HOVSG
中的房间名称原本是直接给定的，而 FSR-VLN 改成让 GPT-4o 根据房间内 View
对应的图像内容推断房间名称。下面是在HOVSG基础上建立HMSG的流程图。</p>
<pre><code class="highlight mermaid">flowchart LR
    A[&quot;HOVSG&lt;br/&gt;Floor–Room–Object&quot;]
    B[&quot;加入 View 节点&quot;]
    C[&quot;View 归属 Room&quot;]
    D[&quot;生成 View 描述&quot;]
    E[&quot;建立 View–Object&lt;br/&gt;可见关系&quot;]
    F[&quot;为 Object 选择&lt;br/&gt;best_view&quot;]
    G[&quot;HMSG&lt;br/&gt;Floor–Room–View–Object&quot;]

    A --&gt; B
    B --&gt; C
    B --&gt; D
    B --&gt; E
    B --&gt; F

    C --&gt; G
    D --&gt; G
    E --&gt; G
    F --&gt; G

    classDef base fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef view fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef process fill:#FFF4D6,stroke:#111111,stroke-width:3px,color:#111111;
    classDef final fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;

    class A base;
    class B view;
    class C,D,E,F process;
    class G final;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<p>HMSG 的拓扑关系可以概括为
<code>Floor → Room → &#123;View, Object&#125;</code>。Floor 与 Room、Room 与
View/Object 之间是层级包含关系，用来组织场景中的空间层次；View 与 Object
之间建立可见关系，表示某个物体可以从哪些历史视角中被观测到；不同 View
之间则根据相对位姿建立无向拓扑边，用来描述机器人在各个历史观测位置之间的空间连接。这样，原本独立的三维物体节点和原始图像视角就通过
View–Object 的关系连接起来。下面是HMSG的各种节点的拓扑关系。</p>
<pre><code class="highlight mermaid">
%%&#123;init: &#123;
  &quot;theme&quot;: &quot;base&quot;,
  &quot;flowchart&quot;: &#123;
    &quot;curve&quot;: &quot;linear&quot;,
    &quot;nodeSpacing&quot;: 45,
    &quot;rankSpacing&quot;: 60
  &#125;
&#125;&#125;%%

flowchart LR
    F[&quot;Floor&quot;]
    R[&quot;Room&quot;]
    V1[&quot;View&quot;]
    V2[&quot;View&quot;]
    O1[&quot;Object&quot;]
    O2[&quot;Object&quot;]

    F --&gt; R

    R --&gt; V1
    R --&gt; V2
    R --&gt; O1
    R --&gt; O2

    V1 --- V2

    V1 --- O1
    V1 --- O2
    V2 --- O2

    O2 -. best_view .-&gt; V2

    classDef floor fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef room fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef view fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef object fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;

    class F floor;
    class R room;
    class V1,V2 view;
    class O1,O2 object;

    linkStyle default stroke:#111111,stroke-width:3px;
</code></pre>
<h3 id="在线推理">在线推理</h3>
<p>在线推理首先由 LLM
对用户指令进行理解。对于“带我去办公室的蓝色圆柱凳”这类显式指令，LLM
会解析出房间和物体；对于“我累了，哪里能休息”这类隐式指令，则先推断用户真正需要寻找的目标物体。如果指令中包含房间信息，系统会先完成
Room 匹配，将后续搜索限制在对应房间内。在此基础上，View 和 Object
两层同时进行 CLIP 相似度匹配：Object 层得到一个候选目标物体，View
层则得到一个候选目标视图。这个阶段基本依赖 embedding
相似度，因此速度很快，但匹配结果不一定可靠。</p>
<p>接下来是 Slow Reasoning。系统取出候选 Object 在建图阶段已经保存好的
<code>best_view</code>，把对应原始图像交给
GPT-4o，判断指令描述的目标是否真的出现在图中。如果确认存在，就直接接受
Fast Matching 的结果；如果判断不可靠，则进行一次视图回退。系统先从其他
View 中筛出一个与目标语义最相关的候选视图，再将它与 Fast Matching
阶段得到的候选 View 进行视觉比较，确定最终目标视图。最后只在这个 View
所关联的 Object 中重新计算 CLIP
相似度，更新目标物体。目标确定之后，三维位置直接取自 Object
节点中已经保存的几何信息，交给后续导航与控制模块。整个回退只执行一次，论文没有设计循环验证或多轮重试。下面是整体的推理流程。</p>
<pre><code class="highlight mermaid">flowchart
    A[&quot;用户指令&quot;] --&gt; B[&quot;LLM 指令理解&quot;]
    B --&gt; C[&quot;Room 范围约束&quot;]

    C --&gt; D[&quot;Object 层&lt;br/&gt;CLIP 匹配&quot;]
    C --&gt; E[&quot;View 层&lt;br/&gt;CLIP 匹配&quot;]

    D --&gt; F[&quot;候选 Object&quot;]
    E --&gt; G[&quot;Fast-matched View&quot;]

    F --&gt; H[&quot;使用 best_view&lt;br/&gt;进行视觉核实&quot;]

    H --&gt;|可靠| I[&quot;确定目标 Object&quot;]

    H --&gt;|不可靠| J[&quot;视图回退&quot;]
    G --&gt; J

    J --&gt; K[&quot;确定最终 View&quot;]
    K --&gt; L[&quot;在关联 Object 中&lt;br/&gt;重新 CLIP 匹配&quot;]
    L --&gt; I

    I --&gt; M[&quot;读取 Object&lt;br/&gt;三维坐标&quot;]

    classDef input fill:#E5DDF7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef fast fill:#DCEBFA,stroke:#111111,stroke-width:3px,color:#111111;
    classDef verify fill:#FFE1B8,stroke:#111111,stroke-width:3px,color:#111111;
    classDef fallback fill:#FFF1C7,stroke:#111111,stroke-width:3px,color:#111111;
    classDef final fill:#DDF3E4,stroke:#111111,stroke-width:3px,color:#111111;

    class A,B input;
    class C,D,E,F,G fast;
    class H verify;
    class J,K,L fallback;
    class I,M final;

    linkStyle default stroke:#111111,stroke-width:3px;</code></pre>
]]></content>
      <categories>
        <category>每月札记</category>
      </categories>
  </entry>
</search>
